listen: 127.0.0.1:8080
db:
  connectionstring: host=localhost port=5432 user=postgres dbname=ccdb password=admin sslmode=disable
  type: postgres
  create: true
  migrate: true
log:
  level: debug
  production: false