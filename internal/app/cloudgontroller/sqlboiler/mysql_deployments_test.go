// +build integration,mysql
// Code generated by SQLBoiler 4.6.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"bytes"
	"context"
	"reflect"
	"testing"

	"github.com/volatiletech/randomize"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testDeployments(t *testing.T) {
	t.Parallel()

	query := Deployments()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testDeploymentsDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Deployment{}
	if err = randomize.Struct(seed, o, deploymentDBTypes, true, deploymentColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Deployment struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := Deployments().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testDeploymentsQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Deployment{}
	if err = randomize.Struct(seed, o, deploymentDBTypes, true, deploymentColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Deployment struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := Deployments().DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := Deployments().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testDeploymentsSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Deployment{}
	if err = randomize.Struct(seed, o, deploymentDBTypes, true, deploymentColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Deployment struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := DeploymentSlice{o}

	if rowsAff, err := slice.DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := Deployments().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testDeploymentsExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Deployment{}
	if err = randomize.Struct(seed, o, deploymentDBTypes, true, deploymentColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Deployment struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := DeploymentExists(ctx, tx, o.ID)
	if err != nil {
		t.Errorf("Unable to check if Deployment exists: %s", err)
	}
	if !e {
		t.Errorf("Expected DeploymentExists to return true, but got false.")
	}
}

func testDeploymentsFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Deployment{}
	if err = randomize.Struct(seed, o, deploymentDBTypes, true, deploymentColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Deployment struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	deploymentFound, err := FindDeployment(ctx, tx, o.ID)
	if err != nil {
		t.Error(err)
	}

	if deploymentFound == nil {
		t.Error("want a record, got nil")
	}
}

func testDeploymentsBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Deployment{}
	if err = randomize.Struct(seed, o, deploymentDBTypes, true, deploymentColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Deployment struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = Deployments().Bind(ctx, tx, o); err != nil {
		t.Error(err)
	}
}

func testDeploymentsOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Deployment{}
	if err = randomize.Struct(seed, o, deploymentDBTypes, true, deploymentColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Deployment struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := Deployments().One(ctx, tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testDeploymentsAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	deploymentOne := &Deployment{}
	deploymentTwo := &Deployment{}
	if err = randomize.Struct(seed, deploymentOne, deploymentDBTypes, false, deploymentColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Deployment struct: %s", err)
	}
	if err = randomize.Struct(seed, deploymentTwo, deploymentDBTypes, false, deploymentColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Deployment struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = deploymentOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = deploymentTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := Deployments().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testDeploymentsCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	deploymentOne := &Deployment{}
	deploymentTwo := &Deployment{}
	if err = randomize.Struct(seed, deploymentOne, deploymentDBTypes, false, deploymentColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Deployment struct: %s", err)
	}
	if err = randomize.Struct(seed, deploymentTwo, deploymentDBTypes, false, deploymentColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Deployment struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = deploymentOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = deploymentTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := Deployments().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func deploymentBeforeInsertHook(ctx context.Context, e boil.ContextExecutor, o *Deployment) error {
	*o = Deployment{}
	return nil
}

func deploymentAfterInsertHook(ctx context.Context, e boil.ContextExecutor, o *Deployment) error {
	*o = Deployment{}
	return nil
}

func deploymentAfterSelectHook(ctx context.Context, e boil.ContextExecutor, o *Deployment) error {
	*o = Deployment{}
	return nil
}

func deploymentBeforeUpdateHook(ctx context.Context, e boil.ContextExecutor, o *Deployment) error {
	*o = Deployment{}
	return nil
}

func deploymentAfterUpdateHook(ctx context.Context, e boil.ContextExecutor, o *Deployment) error {
	*o = Deployment{}
	return nil
}

func deploymentBeforeDeleteHook(ctx context.Context, e boil.ContextExecutor, o *Deployment) error {
	*o = Deployment{}
	return nil
}

func deploymentAfterDeleteHook(ctx context.Context, e boil.ContextExecutor, o *Deployment) error {
	*o = Deployment{}
	return nil
}

func deploymentBeforeUpsertHook(ctx context.Context, e boil.ContextExecutor, o *Deployment) error {
	*o = Deployment{}
	return nil
}

func deploymentAfterUpsertHook(ctx context.Context, e boil.ContextExecutor, o *Deployment) error {
	*o = Deployment{}
	return nil
}

func testDeploymentsHooks(t *testing.T) {
	t.Parallel()

	var err error

	ctx := context.Background()
	empty := &Deployment{}
	o := &Deployment{}

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, o, deploymentDBTypes, false); err != nil {
		t.Errorf("Unable to randomize Deployment object: %s", err)
	}

	AddDeploymentHook(boil.BeforeInsertHook, deploymentBeforeInsertHook)
	if err = o.doBeforeInsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeInsertHook function to empty object, but got: %#v", o)
	}
	deploymentBeforeInsertHooks = []DeploymentHook{}

	AddDeploymentHook(boil.AfterInsertHook, deploymentAfterInsertHook)
	if err = o.doAfterInsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterInsertHook function to empty object, but got: %#v", o)
	}
	deploymentAfterInsertHooks = []DeploymentHook{}

	AddDeploymentHook(boil.AfterSelectHook, deploymentAfterSelectHook)
	if err = o.doAfterSelectHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterSelectHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterSelectHook function to empty object, but got: %#v", o)
	}
	deploymentAfterSelectHooks = []DeploymentHook{}

	AddDeploymentHook(boil.BeforeUpdateHook, deploymentBeforeUpdateHook)
	if err = o.doBeforeUpdateHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpdateHook function to empty object, but got: %#v", o)
	}
	deploymentBeforeUpdateHooks = []DeploymentHook{}

	AddDeploymentHook(boil.AfterUpdateHook, deploymentAfterUpdateHook)
	if err = o.doAfterUpdateHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpdateHook function to empty object, but got: %#v", o)
	}
	deploymentAfterUpdateHooks = []DeploymentHook{}

	AddDeploymentHook(boil.BeforeDeleteHook, deploymentBeforeDeleteHook)
	if err = o.doBeforeDeleteHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeDeleteHook function to empty object, but got: %#v", o)
	}
	deploymentBeforeDeleteHooks = []DeploymentHook{}

	AddDeploymentHook(boil.AfterDeleteHook, deploymentAfterDeleteHook)
	if err = o.doAfterDeleteHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterDeleteHook function to empty object, but got: %#v", o)
	}
	deploymentAfterDeleteHooks = []DeploymentHook{}

	AddDeploymentHook(boil.BeforeUpsertHook, deploymentBeforeUpsertHook)
	if err = o.doBeforeUpsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpsertHook function to empty object, but got: %#v", o)
	}
	deploymentBeforeUpsertHooks = []DeploymentHook{}

	AddDeploymentHook(boil.AfterUpsertHook, deploymentAfterUpsertHook)
	if err = o.doAfterUpsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpsertHook function to empty object, but got: %#v", o)
	}
	deploymentAfterUpsertHooks = []DeploymentHook{}
}

func testDeploymentsInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Deployment{}
	if err = randomize.Struct(seed, o, deploymentDBTypes, true, deploymentColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Deployment struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := Deployments().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testDeploymentsInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Deployment{}
	if err = randomize.Struct(seed, o, deploymentDBTypes, true); err != nil {
		t.Errorf("Unable to randomize Deployment struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Whitelist(deploymentColumnsWithoutDefault...)); err != nil {
		t.Error(err)
	}

	count, err := Deployments().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testDeploymentToManyResourceDeploymentAnnotations(t *testing.T) {
	var err error
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a Deployment
	var b, c DeploymentAnnotation

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, deploymentDBTypes, true, deploymentColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Deployment struct: %s", err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	if err = randomize.Struct(seed, &b, deploymentAnnotationDBTypes, false, deploymentAnnotationColumnsWithDefault...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, deploymentAnnotationDBTypes, false, deploymentAnnotationColumnsWithDefault...); err != nil {
		t.Fatal(err)
	}

	queries.Assign(&b.ResourceGUID, a.GUID)
	queries.Assign(&c.ResourceGUID, a.GUID)
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := a.ResourceDeploymentAnnotations().All(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	bFound, cFound := false, false
	for _, v := range check {
		if queries.Equal(v.ResourceGUID, b.ResourceGUID) {
			bFound = true
		}
		if queries.Equal(v.ResourceGUID, c.ResourceGUID) {
			cFound = true
		}
	}

	if !bFound {
		t.Error("expected to find b")
	}
	if !cFound {
		t.Error("expected to find c")
	}

	slice := DeploymentSlice{&a}
	if err = a.L.LoadResourceDeploymentAnnotations(ctx, tx, false, (*[]*Deployment)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if got := len(a.R.ResourceDeploymentAnnotations); got != 2 {
		t.Error("number of eager loaded records wrong, got:", got)
	}

	a.R.ResourceDeploymentAnnotations = nil
	if err = a.L.LoadResourceDeploymentAnnotations(ctx, tx, true, &a, nil); err != nil {
		t.Fatal(err)
	}
	if got := len(a.R.ResourceDeploymentAnnotations); got != 2 {
		t.Error("number of eager loaded records wrong, got:", got)
	}

	if t.Failed() {
		t.Logf("%#v", check)
	}
}

func testDeploymentToManyResourceDeploymentLabels(t *testing.T) {
	var err error
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a Deployment
	var b, c DeploymentLabel

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, deploymentDBTypes, true, deploymentColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Deployment struct: %s", err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	if err = randomize.Struct(seed, &b, deploymentLabelDBTypes, false, deploymentLabelColumnsWithDefault...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, deploymentLabelDBTypes, false, deploymentLabelColumnsWithDefault...); err != nil {
		t.Fatal(err)
	}

	queries.Assign(&b.ResourceGUID, a.GUID)
	queries.Assign(&c.ResourceGUID, a.GUID)
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := a.ResourceDeploymentLabels().All(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	bFound, cFound := false, false
	for _, v := range check {
		if queries.Equal(v.ResourceGUID, b.ResourceGUID) {
			bFound = true
		}
		if queries.Equal(v.ResourceGUID, c.ResourceGUID) {
			cFound = true
		}
	}

	if !bFound {
		t.Error("expected to find b")
	}
	if !cFound {
		t.Error("expected to find c")
	}

	slice := DeploymentSlice{&a}
	if err = a.L.LoadResourceDeploymentLabels(ctx, tx, false, (*[]*Deployment)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if got := len(a.R.ResourceDeploymentLabels); got != 2 {
		t.Error("number of eager loaded records wrong, got:", got)
	}

	a.R.ResourceDeploymentLabels = nil
	if err = a.L.LoadResourceDeploymentLabels(ctx, tx, true, &a, nil); err != nil {
		t.Fatal(err)
	}
	if got := len(a.R.ResourceDeploymentLabels); got != 2 {
		t.Error("number of eager loaded records wrong, got:", got)
	}

	if t.Failed() {
		t.Logf("%#v", check)
	}
}

func testDeploymentToManyDeploymentProcesses(t *testing.T) {
	var err error
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a Deployment
	var b, c DeploymentProcess

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, deploymentDBTypes, true, deploymentColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Deployment struct: %s", err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	if err = randomize.Struct(seed, &b, deploymentProcessDBTypes, false, deploymentProcessColumnsWithDefault...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, deploymentProcessDBTypes, false, deploymentProcessColumnsWithDefault...); err != nil {
		t.Fatal(err)
	}

	queries.Assign(&b.DeploymentGUID, a.GUID)
	queries.Assign(&c.DeploymentGUID, a.GUID)
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := a.DeploymentProcesses().All(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	bFound, cFound := false, false
	for _, v := range check {
		if queries.Equal(v.DeploymentGUID, b.DeploymentGUID) {
			bFound = true
		}
		if queries.Equal(v.DeploymentGUID, c.DeploymentGUID) {
			cFound = true
		}
	}

	if !bFound {
		t.Error("expected to find b")
	}
	if !cFound {
		t.Error("expected to find c")
	}

	slice := DeploymentSlice{&a}
	if err = a.L.LoadDeploymentProcesses(ctx, tx, false, (*[]*Deployment)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if got := len(a.R.DeploymentProcesses); got != 2 {
		t.Error("number of eager loaded records wrong, got:", got)
	}

	a.R.DeploymentProcesses = nil
	if err = a.L.LoadDeploymentProcesses(ctx, tx, true, &a, nil); err != nil {
		t.Fatal(err)
	}
	if got := len(a.R.DeploymentProcesses); got != 2 {
		t.Error("number of eager loaded records wrong, got:", got)
	}

	if t.Failed() {
		t.Logf("%#v", check)
	}
}

func testDeploymentToManyAddOpResourceDeploymentAnnotations(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a Deployment
	var b, c, d, e DeploymentAnnotation

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, deploymentDBTypes, false, strmangle.SetComplement(deploymentPrimaryKeyColumns, deploymentColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	foreigners := []*DeploymentAnnotation{&b, &c, &d, &e}
	for _, x := range foreigners {
		if err = randomize.Struct(seed, x, deploymentAnnotationDBTypes, false, strmangle.SetComplement(deploymentAnnotationPrimaryKeyColumns, deploymentAnnotationColumnsWithoutDefault)...); err != nil {
			t.Fatal(err)
		}
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	foreignersSplitByInsertion := [][]*DeploymentAnnotation{
		{&b, &c},
		{&d, &e},
	}

	for i, x := range foreignersSplitByInsertion {
		err = a.AddResourceDeploymentAnnotations(ctx, tx, i != 0, x...)
		if err != nil {
			t.Fatal(err)
		}

		first := x[0]
		second := x[1]

		if !queries.Equal(a.GUID, first.ResourceGUID) {
			t.Error("foreign key was wrong value", a.GUID, first.ResourceGUID)
		}
		if !queries.Equal(a.GUID, second.ResourceGUID) {
			t.Error("foreign key was wrong value", a.GUID, second.ResourceGUID)
		}

		if first.R.Resource != &a {
			t.Error("relationship was not added properly to the foreign slice")
		}
		if second.R.Resource != &a {
			t.Error("relationship was not added properly to the foreign slice")
		}

		if a.R.ResourceDeploymentAnnotations[i*2] != first {
			t.Error("relationship struct slice not set to correct value")
		}
		if a.R.ResourceDeploymentAnnotations[i*2+1] != second {
			t.Error("relationship struct slice not set to correct value")
		}

		count, err := a.ResourceDeploymentAnnotations().Count(ctx, tx)
		if err != nil {
			t.Fatal(err)
		}
		if want := int64((i + 1) * 2); count != want {
			t.Error("want", want, "got", count)
		}
	}
}

func testDeploymentToManySetOpResourceDeploymentAnnotations(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a Deployment
	var b, c, d, e DeploymentAnnotation

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, deploymentDBTypes, false, strmangle.SetComplement(deploymentPrimaryKeyColumns, deploymentColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	foreigners := []*DeploymentAnnotation{&b, &c, &d, &e}
	for _, x := range foreigners {
		if err = randomize.Struct(seed, x, deploymentAnnotationDBTypes, false, strmangle.SetComplement(deploymentAnnotationPrimaryKeyColumns, deploymentAnnotationColumnsWithoutDefault)...); err != nil {
			t.Fatal(err)
		}
	}

	if err = a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	err = a.SetResourceDeploymentAnnotations(ctx, tx, false, &b, &c)
	if err != nil {
		t.Fatal(err)
	}

	count, err := a.ResourceDeploymentAnnotations().Count(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}
	if count != 2 {
		t.Error("count was wrong:", count)
	}

	err = a.SetResourceDeploymentAnnotations(ctx, tx, true, &d, &e)
	if err != nil {
		t.Fatal(err)
	}

	count, err = a.ResourceDeploymentAnnotations().Count(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}
	if count != 2 {
		t.Error("count was wrong:", count)
	}

	if !queries.IsValuerNil(b.ResourceGUID) {
		t.Error("want b's foreign key value to be nil")
	}
	if !queries.IsValuerNil(c.ResourceGUID) {
		t.Error("want c's foreign key value to be nil")
	}
	if !queries.Equal(a.GUID, d.ResourceGUID) {
		t.Error("foreign key was wrong value", a.GUID, d.ResourceGUID)
	}
	if !queries.Equal(a.GUID, e.ResourceGUID) {
		t.Error("foreign key was wrong value", a.GUID, e.ResourceGUID)
	}

	if b.R.Resource != nil {
		t.Error("relationship was not removed properly from the foreign struct")
	}
	if c.R.Resource != nil {
		t.Error("relationship was not removed properly from the foreign struct")
	}
	if d.R.Resource != &a {
		t.Error("relationship was not added properly to the foreign struct")
	}
	if e.R.Resource != &a {
		t.Error("relationship was not added properly to the foreign struct")
	}

	if a.R.ResourceDeploymentAnnotations[0] != &d {
		t.Error("relationship struct slice not set to correct value")
	}
	if a.R.ResourceDeploymentAnnotations[1] != &e {
		t.Error("relationship struct slice not set to correct value")
	}
}

func testDeploymentToManyRemoveOpResourceDeploymentAnnotations(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a Deployment
	var b, c, d, e DeploymentAnnotation

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, deploymentDBTypes, false, strmangle.SetComplement(deploymentPrimaryKeyColumns, deploymentColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	foreigners := []*DeploymentAnnotation{&b, &c, &d, &e}
	for _, x := range foreigners {
		if err = randomize.Struct(seed, x, deploymentAnnotationDBTypes, false, strmangle.SetComplement(deploymentAnnotationPrimaryKeyColumns, deploymentAnnotationColumnsWithoutDefault)...); err != nil {
			t.Fatal(err)
		}
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	err = a.AddResourceDeploymentAnnotations(ctx, tx, true, foreigners...)
	if err != nil {
		t.Fatal(err)
	}

	count, err := a.ResourceDeploymentAnnotations().Count(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}
	if count != 4 {
		t.Error("count was wrong:", count)
	}

	err = a.RemoveResourceDeploymentAnnotations(ctx, tx, foreigners[:2]...)
	if err != nil {
		t.Fatal(err)
	}

	count, err = a.ResourceDeploymentAnnotations().Count(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}
	if count != 2 {
		t.Error("count was wrong:", count)
	}

	if !queries.IsValuerNil(b.ResourceGUID) {
		t.Error("want b's foreign key value to be nil")
	}
	if !queries.IsValuerNil(c.ResourceGUID) {
		t.Error("want c's foreign key value to be nil")
	}

	if b.R.Resource != nil {
		t.Error("relationship was not removed properly from the foreign struct")
	}
	if c.R.Resource != nil {
		t.Error("relationship was not removed properly from the foreign struct")
	}
	if d.R.Resource != &a {
		t.Error("relationship to a should have been preserved")
	}
	if e.R.Resource != &a {
		t.Error("relationship to a should have been preserved")
	}

	if len(a.R.ResourceDeploymentAnnotations) != 2 {
		t.Error("should have preserved two relationships")
	}

	// Removal doesn't do a stable deletion for performance so we have to flip the order
	if a.R.ResourceDeploymentAnnotations[1] != &d {
		t.Error("relationship to d should have been preserved")
	}
	if a.R.ResourceDeploymentAnnotations[0] != &e {
		t.Error("relationship to e should have been preserved")
	}
}

func testDeploymentToManyAddOpResourceDeploymentLabels(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a Deployment
	var b, c, d, e DeploymentLabel

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, deploymentDBTypes, false, strmangle.SetComplement(deploymentPrimaryKeyColumns, deploymentColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	foreigners := []*DeploymentLabel{&b, &c, &d, &e}
	for _, x := range foreigners {
		if err = randomize.Struct(seed, x, deploymentLabelDBTypes, false, strmangle.SetComplement(deploymentLabelPrimaryKeyColumns, deploymentLabelColumnsWithoutDefault)...); err != nil {
			t.Fatal(err)
		}
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	foreignersSplitByInsertion := [][]*DeploymentLabel{
		{&b, &c},
		{&d, &e},
	}

	for i, x := range foreignersSplitByInsertion {
		err = a.AddResourceDeploymentLabels(ctx, tx, i != 0, x...)
		if err != nil {
			t.Fatal(err)
		}

		first := x[0]
		second := x[1]

		if !queries.Equal(a.GUID, first.ResourceGUID) {
			t.Error("foreign key was wrong value", a.GUID, first.ResourceGUID)
		}
		if !queries.Equal(a.GUID, second.ResourceGUID) {
			t.Error("foreign key was wrong value", a.GUID, second.ResourceGUID)
		}

		if first.R.Resource != &a {
			t.Error("relationship was not added properly to the foreign slice")
		}
		if second.R.Resource != &a {
			t.Error("relationship was not added properly to the foreign slice")
		}

		if a.R.ResourceDeploymentLabels[i*2] != first {
			t.Error("relationship struct slice not set to correct value")
		}
		if a.R.ResourceDeploymentLabels[i*2+1] != second {
			t.Error("relationship struct slice not set to correct value")
		}

		count, err := a.ResourceDeploymentLabels().Count(ctx, tx)
		if err != nil {
			t.Fatal(err)
		}
		if want := int64((i + 1) * 2); count != want {
			t.Error("want", want, "got", count)
		}
	}
}

func testDeploymentToManySetOpResourceDeploymentLabels(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a Deployment
	var b, c, d, e DeploymentLabel

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, deploymentDBTypes, false, strmangle.SetComplement(deploymentPrimaryKeyColumns, deploymentColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	foreigners := []*DeploymentLabel{&b, &c, &d, &e}
	for _, x := range foreigners {
		if err = randomize.Struct(seed, x, deploymentLabelDBTypes, false, strmangle.SetComplement(deploymentLabelPrimaryKeyColumns, deploymentLabelColumnsWithoutDefault)...); err != nil {
			t.Fatal(err)
		}
	}

	if err = a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	err = a.SetResourceDeploymentLabels(ctx, tx, false, &b, &c)
	if err != nil {
		t.Fatal(err)
	}

	count, err := a.ResourceDeploymentLabels().Count(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}
	if count != 2 {
		t.Error("count was wrong:", count)
	}

	err = a.SetResourceDeploymentLabels(ctx, tx, true, &d, &e)
	if err != nil {
		t.Fatal(err)
	}

	count, err = a.ResourceDeploymentLabels().Count(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}
	if count != 2 {
		t.Error("count was wrong:", count)
	}

	if !queries.IsValuerNil(b.ResourceGUID) {
		t.Error("want b's foreign key value to be nil")
	}
	if !queries.IsValuerNil(c.ResourceGUID) {
		t.Error("want c's foreign key value to be nil")
	}
	if !queries.Equal(a.GUID, d.ResourceGUID) {
		t.Error("foreign key was wrong value", a.GUID, d.ResourceGUID)
	}
	if !queries.Equal(a.GUID, e.ResourceGUID) {
		t.Error("foreign key was wrong value", a.GUID, e.ResourceGUID)
	}

	if b.R.Resource != nil {
		t.Error("relationship was not removed properly from the foreign struct")
	}
	if c.R.Resource != nil {
		t.Error("relationship was not removed properly from the foreign struct")
	}
	if d.R.Resource != &a {
		t.Error("relationship was not added properly to the foreign struct")
	}
	if e.R.Resource != &a {
		t.Error("relationship was not added properly to the foreign struct")
	}

	if a.R.ResourceDeploymentLabels[0] != &d {
		t.Error("relationship struct slice not set to correct value")
	}
	if a.R.ResourceDeploymentLabels[1] != &e {
		t.Error("relationship struct slice not set to correct value")
	}
}

func testDeploymentToManyRemoveOpResourceDeploymentLabels(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a Deployment
	var b, c, d, e DeploymentLabel

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, deploymentDBTypes, false, strmangle.SetComplement(deploymentPrimaryKeyColumns, deploymentColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	foreigners := []*DeploymentLabel{&b, &c, &d, &e}
	for _, x := range foreigners {
		if err = randomize.Struct(seed, x, deploymentLabelDBTypes, false, strmangle.SetComplement(deploymentLabelPrimaryKeyColumns, deploymentLabelColumnsWithoutDefault)...); err != nil {
			t.Fatal(err)
		}
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	err = a.AddResourceDeploymentLabels(ctx, tx, true, foreigners...)
	if err != nil {
		t.Fatal(err)
	}

	count, err := a.ResourceDeploymentLabels().Count(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}
	if count != 4 {
		t.Error("count was wrong:", count)
	}

	err = a.RemoveResourceDeploymentLabels(ctx, tx, foreigners[:2]...)
	if err != nil {
		t.Fatal(err)
	}

	count, err = a.ResourceDeploymentLabels().Count(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}
	if count != 2 {
		t.Error("count was wrong:", count)
	}

	if !queries.IsValuerNil(b.ResourceGUID) {
		t.Error("want b's foreign key value to be nil")
	}
	if !queries.IsValuerNil(c.ResourceGUID) {
		t.Error("want c's foreign key value to be nil")
	}

	if b.R.Resource != nil {
		t.Error("relationship was not removed properly from the foreign struct")
	}
	if c.R.Resource != nil {
		t.Error("relationship was not removed properly from the foreign struct")
	}
	if d.R.Resource != &a {
		t.Error("relationship to a should have been preserved")
	}
	if e.R.Resource != &a {
		t.Error("relationship to a should have been preserved")
	}

	if len(a.R.ResourceDeploymentLabels) != 2 {
		t.Error("should have preserved two relationships")
	}

	// Removal doesn't do a stable deletion for performance so we have to flip the order
	if a.R.ResourceDeploymentLabels[1] != &d {
		t.Error("relationship to d should have been preserved")
	}
	if a.R.ResourceDeploymentLabels[0] != &e {
		t.Error("relationship to e should have been preserved")
	}
}

func testDeploymentToManyAddOpDeploymentProcesses(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a Deployment
	var b, c, d, e DeploymentProcess

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, deploymentDBTypes, false, strmangle.SetComplement(deploymentPrimaryKeyColumns, deploymentColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	foreigners := []*DeploymentProcess{&b, &c, &d, &e}
	for _, x := range foreigners {
		if err = randomize.Struct(seed, x, deploymentProcessDBTypes, false, strmangle.SetComplement(deploymentProcessPrimaryKeyColumns, deploymentProcessColumnsWithoutDefault)...); err != nil {
			t.Fatal(err)
		}
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	foreignersSplitByInsertion := [][]*DeploymentProcess{
		{&b, &c},
		{&d, &e},
	}

	for i, x := range foreignersSplitByInsertion {
		err = a.AddDeploymentProcesses(ctx, tx, i != 0, x...)
		if err != nil {
			t.Fatal(err)
		}

		first := x[0]
		second := x[1]

		if !queries.Equal(a.GUID, first.DeploymentGUID) {
			t.Error("foreign key was wrong value", a.GUID, first.DeploymentGUID)
		}
		if !queries.Equal(a.GUID, second.DeploymentGUID) {
			t.Error("foreign key was wrong value", a.GUID, second.DeploymentGUID)
		}

		if first.R.Deployment != &a {
			t.Error("relationship was not added properly to the foreign slice")
		}
		if second.R.Deployment != &a {
			t.Error("relationship was not added properly to the foreign slice")
		}

		if a.R.DeploymentProcesses[i*2] != first {
			t.Error("relationship struct slice not set to correct value")
		}
		if a.R.DeploymentProcesses[i*2+1] != second {
			t.Error("relationship struct slice not set to correct value")
		}

		count, err := a.DeploymentProcesses().Count(ctx, tx)
		if err != nil {
			t.Fatal(err)
		}
		if want := int64((i + 1) * 2); count != want {
			t.Error("want", want, "got", count)
		}
	}
}

func testDeploymentToManySetOpDeploymentProcesses(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a Deployment
	var b, c, d, e DeploymentProcess

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, deploymentDBTypes, false, strmangle.SetComplement(deploymentPrimaryKeyColumns, deploymentColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	foreigners := []*DeploymentProcess{&b, &c, &d, &e}
	for _, x := range foreigners {
		if err = randomize.Struct(seed, x, deploymentProcessDBTypes, false, strmangle.SetComplement(deploymentProcessPrimaryKeyColumns, deploymentProcessColumnsWithoutDefault)...); err != nil {
			t.Fatal(err)
		}
	}

	if err = a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	err = a.SetDeploymentProcesses(ctx, tx, false, &b, &c)
	if err != nil {
		t.Fatal(err)
	}

	count, err := a.DeploymentProcesses().Count(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}
	if count != 2 {
		t.Error("count was wrong:", count)
	}

	err = a.SetDeploymentProcesses(ctx, tx, true, &d, &e)
	if err != nil {
		t.Fatal(err)
	}

	count, err = a.DeploymentProcesses().Count(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}
	if count != 2 {
		t.Error("count was wrong:", count)
	}

	if !queries.IsValuerNil(b.DeploymentGUID) {
		t.Error("want b's foreign key value to be nil")
	}
	if !queries.IsValuerNil(c.DeploymentGUID) {
		t.Error("want c's foreign key value to be nil")
	}
	if !queries.Equal(a.GUID, d.DeploymentGUID) {
		t.Error("foreign key was wrong value", a.GUID, d.DeploymentGUID)
	}
	if !queries.Equal(a.GUID, e.DeploymentGUID) {
		t.Error("foreign key was wrong value", a.GUID, e.DeploymentGUID)
	}

	if b.R.Deployment != nil {
		t.Error("relationship was not removed properly from the foreign struct")
	}
	if c.R.Deployment != nil {
		t.Error("relationship was not removed properly from the foreign struct")
	}
	if d.R.Deployment != &a {
		t.Error("relationship was not added properly to the foreign struct")
	}
	if e.R.Deployment != &a {
		t.Error("relationship was not added properly to the foreign struct")
	}

	if a.R.DeploymentProcesses[0] != &d {
		t.Error("relationship struct slice not set to correct value")
	}
	if a.R.DeploymentProcesses[1] != &e {
		t.Error("relationship struct slice not set to correct value")
	}
}

func testDeploymentToManyRemoveOpDeploymentProcesses(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a Deployment
	var b, c, d, e DeploymentProcess

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, deploymentDBTypes, false, strmangle.SetComplement(deploymentPrimaryKeyColumns, deploymentColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	foreigners := []*DeploymentProcess{&b, &c, &d, &e}
	for _, x := range foreigners {
		if err = randomize.Struct(seed, x, deploymentProcessDBTypes, false, strmangle.SetComplement(deploymentProcessPrimaryKeyColumns, deploymentProcessColumnsWithoutDefault)...); err != nil {
			t.Fatal(err)
		}
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	err = a.AddDeploymentProcesses(ctx, tx, true, foreigners...)
	if err != nil {
		t.Fatal(err)
	}

	count, err := a.DeploymentProcesses().Count(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}
	if count != 4 {
		t.Error("count was wrong:", count)
	}

	err = a.RemoveDeploymentProcesses(ctx, tx, foreigners[:2]...)
	if err != nil {
		t.Fatal(err)
	}

	count, err = a.DeploymentProcesses().Count(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}
	if count != 2 {
		t.Error("count was wrong:", count)
	}

	if !queries.IsValuerNil(b.DeploymentGUID) {
		t.Error("want b's foreign key value to be nil")
	}
	if !queries.IsValuerNil(c.DeploymentGUID) {
		t.Error("want c's foreign key value to be nil")
	}

	if b.R.Deployment != nil {
		t.Error("relationship was not removed properly from the foreign struct")
	}
	if c.R.Deployment != nil {
		t.Error("relationship was not removed properly from the foreign struct")
	}
	if d.R.Deployment != &a {
		t.Error("relationship to a should have been preserved")
	}
	if e.R.Deployment != &a {
		t.Error("relationship to a should have been preserved")
	}

	if len(a.R.DeploymentProcesses) != 2 {
		t.Error("should have preserved two relationships")
	}

	// Removal doesn't do a stable deletion for performance so we have to flip the order
	if a.R.DeploymentProcesses[1] != &d {
		t.Error("relationship to d should have been preserved")
	}
	if a.R.DeploymentProcesses[0] != &e {
		t.Error("relationship to e should have been preserved")
	}
}

func testDeploymentToOneAppUsingApp(t *testing.T) {
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var local Deployment
	var foreign App

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, deploymentDBTypes, true, deploymentColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Deployment struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, appDBTypes, false, appColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize App struct: %s", err)
	}

	if err := foreign.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	queries.Assign(&local.AppGUID, foreign.GUID)
	if err := local.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := local.App().One(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	if !queries.Equal(check.GUID, foreign.GUID) {
		t.Errorf("want: %v, got %v", foreign.GUID, check.GUID)
	}

	slice := DeploymentSlice{&local}
	if err = local.L.LoadApp(ctx, tx, false, (*[]*Deployment)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if local.R.App == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.App = nil
	if err = local.L.LoadApp(ctx, tx, true, &local, nil); err != nil {
		t.Fatal(err)
	}
	if local.R.App == nil {
		t.Error("struct should have been eager loaded")
	}
}

func testDeploymentToOneSetOpAppUsingApp(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a Deployment
	var b, c App

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, deploymentDBTypes, false, strmangle.SetComplement(deploymentPrimaryKeyColumns, deploymentColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, appDBTypes, false, strmangle.SetComplement(appPrimaryKeyColumns, appColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, appDBTypes, false, strmangle.SetComplement(appPrimaryKeyColumns, appColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*App{&b, &c} {
		err = a.SetApp(ctx, tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.App != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.Deployments[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if !queries.Equal(a.AppGUID, x.GUID) {
			t.Error("foreign key was wrong value", a.AppGUID)
		}

		zero := reflect.Zero(reflect.TypeOf(a.AppGUID))
		reflect.Indirect(reflect.ValueOf(&a.AppGUID)).Set(zero)

		if err = a.Reload(ctx, tx); err != nil {
			t.Fatal("failed to reload", err)
		}

		if !queries.Equal(a.AppGUID, x.GUID) {
			t.Error("foreign key was wrong value", a.AppGUID, x.GUID)
		}
	}
}

func testDeploymentToOneRemoveOpAppUsingApp(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a Deployment
	var b App

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, deploymentDBTypes, false, strmangle.SetComplement(deploymentPrimaryKeyColumns, deploymentColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, appDBTypes, false, strmangle.SetComplement(appPrimaryKeyColumns, appColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err = a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	if err = a.SetApp(ctx, tx, true, &b); err != nil {
		t.Fatal(err)
	}

	if err = a.RemoveApp(ctx, tx, &b); err != nil {
		t.Error("failed to remove relationship")
	}

	count, err := a.App().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 0 {
		t.Error("want no relationships remaining")
	}

	if a.R.App != nil {
		t.Error("R struct entry should be nil")
	}

	if !queries.IsValuerNil(a.AppGUID) {
		t.Error("foreign key value should be nil")
	}

	if len(b.R.Deployments) != 0 {
		t.Error("failed to remove a from b's relationships")
	}
}

func testDeploymentsReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Deployment{}
	if err = randomize.Struct(seed, o, deploymentDBTypes, true, deploymentColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Deployment struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testDeploymentsReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Deployment{}
	if err = randomize.Struct(seed, o, deploymentDBTypes, true, deploymentColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Deployment struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := DeploymentSlice{o}

	if err = slice.ReloadAll(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testDeploymentsSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Deployment{}
	if err = randomize.Struct(seed, o, deploymentDBTypes, true, deploymentColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Deployment struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := Deployments().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	deploymentDBTypes = map[string]string{`ID`: `int`, `GUID`: `varchar`, `CreatedAt`: `timestamp`, `UpdatedAt`: `timestamp`, `State`: `varchar`, `AppGUID`: `varchar`, `DropletGUID`: `varchar`, `DeployingWebProcessGUID`: `varchar`, `PreviousDropletGUID`: `varchar`, `OriginalWebProcessInstanceCount`: `int`, `RevisionGUID`: `varchar`, `RevisionVersion`: `int`, `LastHealthyAt`: `timestamp`, `StatusValue`: `varchar`, `StatusReason`: `varchar`, `Strategy`: `varchar`}
	_                 = bytes.MinRead
)

func testDeploymentsUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(deploymentPrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(deploymentAllColumns) == len(deploymentPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &Deployment{}
	if err = randomize.Struct(seed, o, deploymentDBTypes, true, deploymentColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Deployment struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := Deployments().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, deploymentDBTypes, true, deploymentPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize Deployment struct: %s", err)
	}

	if rowsAff, err := o.Update(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testDeploymentsSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(deploymentAllColumns) == len(deploymentPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &Deployment{}
	if err = randomize.Struct(seed, o, deploymentDBTypes, true, deploymentColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Deployment struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := Deployments().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, deploymentDBTypes, true, deploymentPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize Deployment struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(deploymentAllColumns, deploymentPrimaryKeyColumns) {
		fields = deploymentAllColumns
	} else {
		fields = strmangle.SetComplement(
			deploymentAllColumns,
			deploymentPrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := DeploymentSlice{o}
	if rowsAff, err := slice.UpdateAll(ctx, tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testDeploymentsUpsert(t *testing.T) {
	t.Parallel()

	if len(deploymentAllColumns) == len(deploymentPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}
	if len(mySQLDeploymentUniqueColumns) == 0 {
		t.Skip("Skipping table with no unique columns to conflict on")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := Deployment{}
	if err = randomize.Struct(seed, &o, deploymentDBTypes, false); err != nil {
		t.Errorf("Unable to randomize Deployment struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(ctx, tx, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert Deployment: %s", err)
	}

	count, err := Deployments().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, deploymentDBTypes, false, deploymentPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize Deployment struct: %s", err)
	}

	if err = o.Upsert(ctx, tx, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert Deployment: %s", err)
	}

	count, err = Deployments().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
