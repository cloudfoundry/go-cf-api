// +build psql,db
// Code generated by SQLBoiler 4.6.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"bytes"
	"context"
	"reflect"
	"testing"

	"github.com/volatiletech/randomize"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testUserLabels(t *testing.T) {
	t.Parallel()

	query := UserLabels()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testUserLabelsDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserLabel{}
	if err = randomize.Struct(seed, o, userLabelDBTypes, true, userLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := UserLabels().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testUserLabelsQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserLabel{}
	if err = randomize.Struct(seed, o, userLabelDBTypes, true, userLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := UserLabels().DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := UserLabels().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testUserLabelsSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserLabel{}
	if err = randomize.Struct(seed, o, userLabelDBTypes, true, userLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := UserLabelSlice{o}

	if rowsAff, err := slice.DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := UserLabels().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testUserLabelsExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserLabel{}
	if err = randomize.Struct(seed, o, userLabelDBTypes, true, userLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := UserLabelExists(ctx, tx, o.ID)
	if err != nil {
		t.Errorf("Unable to check if UserLabel exists: %s", err)
	}
	if !e {
		t.Errorf("Expected UserLabelExists to return true, but got false.")
	}
}

func testUserLabelsFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserLabel{}
	if err = randomize.Struct(seed, o, userLabelDBTypes, true, userLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	userLabelFound, err := FindUserLabel(ctx, tx, o.ID)
	if err != nil {
		t.Error(err)
	}

	if userLabelFound == nil {
		t.Error("want a record, got nil")
	}
}

func testUserLabelsBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserLabel{}
	if err = randomize.Struct(seed, o, userLabelDBTypes, true, userLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = UserLabels().Bind(ctx, tx, o); err != nil {
		t.Error(err)
	}
}

func testUserLabelsOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserLabel{}
	if err = randomize.Struct(seed, o, userLabelDBTypes, true, userLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := UserLabels().One(ctx, tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testUserLabelsAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	userLabelOne := &UserLabel{}
	userLabelTwo := &UserLabel{}
	if err = randomize.Struct(seed, userLabelOne, userLabelDBTypes, false, userLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserLabel struct: %s", err)
	}
	if err = randomize.Struct(seed, userLabelTwo, userLabelDBTypes, false, userLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = userLabelOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = userLabelTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := UserLabels().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testUserLabelsCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	userLabelOne := &UserLabel{}
	userLabelTwo := &UserLabel{}
	if err = randomize.Struct(seed, userLabelOne, userLabelDBTypes, false, userLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserLabel struct: %s", err)
	}
	if err = randomize.Struct(seed, userLabelTwo, userLabelDBTypes, false, userLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = userLabelOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = userLabelTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := UserLabels().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func userLabelBeforeInsertHook(ctx context.Context, e boil.ContextExecutor, o *UserLabel) error {
	*o = UserLabel{}
	return nil
}

func userLabelAfterInsertHook(ctx context.Context, e boil.ContextExecutor, o *UserLabel) error {
	*o = UserLabel{}
	return nil
}

func userLabelAfterSelectHook(ctx context.Context, e boil.ContextExecutor, o *UserLabel) error {
	*o = UserLabel{}
	return nil
}

func userLabelBeforeUpdateHook(ctx context.Context, e boil.ContextExecutor, o *UserLabel) error {
	*o = UserLabel{}
	return nil
}

func userLabelAfterUpdateHook(ctx context.Context, e boil.ContextExecutor, o *UserLabel) error {
	*o = UserLabel{}
	return nil
}

func userLabelBeforeDeleteHook(ctx context.Context, e boil.ContextExecutor, o *UserLabel) error {
	*o = UserLabel{}
	return nil
}

func userLabelAfterDeleteHook(ctx context.Context, e boil.ContextExecutor, o *UserLabel) error {
	*o = UserLabel{}
	return nil
}

func userLabelBeforeUpsertHook(ctx context.Context, e boil.ContextExecutor, o *UserLabel) error {
	*o = UserLabel{}
	return nil
}

func userLabelAfterUpsertHook(ctx context.Context, e boil.ContextExecutor, o *UserLabel) error {
	*o = UserLabel{}
	return nil
}

func testUserLabelsHooks(t *testing.T) {
	t.Parallel()

	var err error

	ctx := context.Background()
	empty := &UserLabel{}
	o := &UserLabel{}

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, o, userLabelDBTypes, false); err != nil {
		t.Errorf("Unable to randomize UserLabel object: %s", err)
	}

	AddUserLabelHook(boil.BeforeInsertHook, userLabelBeforeInsertHook)
	if err = o.doBeforeInsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeInsertHook function to empty object, but got: %#v", o)
	}
	userLabelBeforeInsertHooks = []UserLabelHook{}

	AddUserLabelHook(boil.AfterInsertHook, userLabelAfterInsertHook)
	if err = o.doAfterInsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterInsertHook function to empty object, but got: %#v", o)
	}
	userLabelAfterInsertHooks = []UserLabelHook{}

	AddUserLabelHook(boil.AfterSelectHook, userLabelAfterSelectHook)
	if err = o.doAfterSelectHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterSelectHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterSelectHook function to empty object, but got: %#v", o)
	}
	userLabelAfterSelectHooks = []UserLabelHook{}

	AddUserLabelHook(boil.BeforeUpdateHook, userLabelBeforeUpdateHook)
	if err = o.doBeforeUpdateHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpdateHook function to empty object, but got: %#v", o)
	}
	userLabelBeforeUpdateHooks = []UserLabelHook{}

	AddUserLabelHook(boil.AfterUpdateHook, userLabelAfterUpdateHook)
	if err = o.doAfterUpdateHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpdateHook function to empty object, but got: %#v", o)
	}
	userLabelAfterUpdateHooks = []UserLabelHook{}

	AddUserLabelHook(boil.BeforeDeleteHook, userLabelBeforeDeleteHook)
	if err = o.doBeforeDeleteHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeDeleteHook function to empty object, but got: %#v", o)
	}
	userLabelBeforeDeleteHooks = []UserLabelHook{}

	AddUserLabelHook(boil.AfterDeleteHook, userLabelAfterDeleteHook)
	if err = o.doAfterDeleteHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterDeleteHook function to empty object, but got: %#v", o)
	}
	userLabelAfterDeleteHooks = []UserLabelHook{}

	AddUserLabelHook(boil.BeforeUpsertHook, userLabelBeforeUpsertHook)
	if err = o.doBeforeUpsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpsertHook function to empty object, but got: %#v", o)
	}
	userLabelBeforeUpsertHooks = []UserLabelHook{}

	AddUserLabelHook(boil.AfterUpsertHook, userLabelAfterUpsertHook)
	if err = o.doAfterUpsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpsertHook function to empty object, but got: %#v", o)
	}
	userLabelAfterUpsertHooks = []UserLabelHook{}
}

func testUserLabelsInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserLabel{}
	if err = randomize.Struct(seed, o, userLabelDBTypes, true, userLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := UserLabels().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testUserLabelsInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserLabel{}
	if err = randomize.Struct(seed, o, userLabelDBTypes, true); err != nil {
		t.Errorf("Unable to randomize UserLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Whitelist(userLabelColumnsWithoutDefault...)); err != nil {
		t.Error(err)
	}

	count, err := UserLabels().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testUserLabelToOneUserUsingResource(t *testing.T) {
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var local UserLabel
	var foreign User

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, userLabelDBTypes, true, userLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserLabel struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, userDBTypes, false, userColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize User struct: %s", err)
	}

	if err := foreign.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	queries.Assign(&local.ResourceGUID, foreign.GUID)
	if err := local.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := local.Resource().One(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	if !queries.Equal(check.GUID, foreign.GUID) {
		t.Errorf("want: %v, got %v", foreign.GUID, check.GUID)
	}

	slice := UserLabelSlice{&local}
	if err = local.L.LoadResource(ctx, tx, false, (*[]*UserLabel)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Resource == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.Resource = nil
	if err = local.L.LoadResource(ctx, tx, true, &local, nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Resource == nil {
		t.Error("struct should have been eager loaded")
	}
}

func testUserLabelToOneSetOpUserUsingResource(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a UserLabel
	var b, c User

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, userLabelDBTypes, false, strmangle.SetComplement(userLabelPrimaryKeyColumns, userLabelColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, userDBTypes, false, strmangle.SetComplement(userPrimaryKeyColumns, userColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, userDBTypes, false, strmangle.SetComplement(userPrimaryKeyColumns, userColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*User{&b, &c} {
		err = a.SetResource(ctx, tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.Resource != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.ResourceUserLabels[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if !queries.Equal(a.ResourceGUID, x.GUID) {
			t.Error("foreign key was wrong value", a.ResourceGUID)
		}

		zero := reflect.Zero(reflect.TypeOf(a.ResourceGUID))
		reflect.Indirect(reflect.ValueOf(&a.ResourceGUID)).Set(zero)

		if err = a.Reload(ctx, tx); err != nil {
			t.Fatal("failed to reload", err)
		}

		if !queries.Equal(a.ResourceGUID, x.GUID) {
			t.Error("foreign key was wrong value", a.ResourceGUID, x.GUID)
		}
	}
}

func testUserLabelToOneRemoveOpUserUsingResource(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a UserLabel
	var b User

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, userLabelDBTypes, false, strmangle.SetComplement(userLabelPrimaryKeyColumns, userLabelColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, userDBTypes, false, strmangle.SetComplement(userPrimaryKeyColumns, userColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err = a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	if err = a.SetResource(ctx, tx, true, &b); err != nil {
		t.Fatal(err)
	}

	if err = a.RemoveResource(ctx, tx, &b); err != nil {
		t.Error("failed to remove relationship")
	}

	count, err := a.Resource().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 0 {
		t.Error("want no relationships remaining")
	}

	if a.R.Resource != nil {
		t.Error("R struct entry should be nil")
	}

	if !queries.IsValuerNil(a.ResourceGUID) {
		t.Error("foreign key value should be nil")
	}

	if len(b.R.ResourceUserLabels) != 0 {
		t.Error("failed to remove a from b's relationships")
	}
}

func testUserLabelsReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserLabel{}
	if err = randomize.Struct(seed, o, userLabelDBTypes, true, userLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testUserLabelsReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserLabel{}
	if err = randomize.Struct(seed, o, userLabelDBTypes, true, userLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := UserLabelSlice{o}

	if err = slice.ReloadAll(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testUserLabelsSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserLabel{}
	if err = randomize.Struct(seed, o, userLabelDBTypes, true, userLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := UserLabels().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	userLabelDBTypes = map[string]string{`ID`: `integer`, `GUID`: `text`, `CreatedAt`: `timestamp without time zone`, `UpdatedAt`: `timestamp without time zone`, `ResourceGUID`: `character varying`, `KeyPrefix`: `character varying`, `KeyName`: `character varying`, `Value`: `character varying`}
	_                = bytes.MinRead
)

func testUserLabelsUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(userLabelPrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(userLabelAllColumns) == len(userLabelPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &UserLabel{}
	if err = randomize.Struct(seed, o, userLabelDBTypes, true, userLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := UserLabels().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, userLabelDBTypes, true, userLabelPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize UserLabel struct: %s", err)
	}

	if rowsAff, err := o.Update(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testUserLabelsSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(userLabelAllColumns) == len(userLabelPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &UserLabel{}
	if err = randomize.Struct(seed, o, userLabelDBTypes, true, userLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := UserLabels().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, userLabelDBTypes, true, userLabelPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize UserLabel struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(userLabelAllColumns, userLabelPrimaryKeyColumns) {
		fields = userLabelAllColumns
	} else {
		fields = strmangle.SetComplement(
			userLabelAllColumns,
			userLabelPrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := UserLabelSlice{o}
	if rowsAff, err := slice.UpdateAll(ctx, tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testUserLabelsUpsert(t *testing.T) {
	t.Parallel()

	if len(userLabelAllColumns) == len(userLabelPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := UserLabel{}
	if err = randomize.Struct(seed, &o, userLabelDBTypes, true); err != nil {
		t.Errorf("Unable to randomize UserLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(ctx, tx, false, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert UserLabel: %s", err)
	}

	count, err := UserLabels().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, userLabelDBTypes, false, userLabelPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize UserLabel struct: %s", err)
	}

	if err = o.Upsert(ctx, tx, true, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert UserLabel: %s", err)
	}

	count, err = UserLabels().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
