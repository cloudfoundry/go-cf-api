// +build integration postgres
// Code generated by SQLBoiler 4.6.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"bytes"
	"context"
	"reflect"
	"testing"

	"github.com/volatiletech/randomize"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testDeploymentLabels(t *testing.T) {
	t.Parallel()

	query := DeploymentLabels()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testDeploymentLabelsDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DeploymentLabel{}
	if err = randomize.Struct(seed, o, deploymentLabelDBTypes, true, deploymentLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DeploymentLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := DeploymentLabels().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testDeploymentLabelsQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DeploymentLabel{}
	if err = randomize.Struct(seed, o, deploymentLabelDBTypes, true, deploymentLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DeploymentLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := DeploymentLabels().DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := DeploymentLabels().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testDeploymentLabelsSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DeploymentLabel{}
	if err = randomize.Struct(seed, o, deploymentLabelDBTypes, true, deploymentLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DeploymentLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := DeploymentLabelSlice{o}

	if rowsAff, err := slice.DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := DeploymentLabels().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testDeploymentLabelsExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DeploymentLabel{}
	if err = randomize.Struct(seed, o, deploymentLabelDBTypes, true, deploymentLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DeploymentLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := DeploymentLabelExists(ctx, tx, o.ID)
	if err != nil {
		t.Errorf("Unable to check if DeploymentLabel exists: %s", err)
	}
	if !e {
		t.Errorf("Expected DeploymentLabelExists to return true, but got false.")
	}
}

func testDeploymentLabelsFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DeploymentLabel{}
	if err = randomize.Struct(seed, o, deploymentLabelDBTypes, true, deploymentLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DeploymentLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	deploymentLabelFound, err := FindDeploymentLabel(ctx, tx, o.ID)
	if err != nil {
		t.Error(err)
	}

	if deploymentLabelFound == nil {
		t.Error("want a record, got nil")
	}
}

func testDeploymentLabelsBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DeploymentLabel{}
	if err = randomize.Struct(seed, o, deploymentLabelDBTypes, true, deploymentLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DeploymentLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = DeploymentLabels().Bind(ctx, tx, o); err != nil {
		t.Error(err)
	}
}

func testDeploymentLabelsOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DeploymentLabel{}
	if err = randomize.Struct(seed, o, deploymentLabelDBTypes, true, deploymentLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DeploymentLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := DeploymentLabels().One(ctx, tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testDeploymentLabelsAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	deploymentLabelOne := &DeploymentLabel{}
	deploymentLabelTwo := &DeploymentLabel{}
	if err = randomize.Struct(seed, deploymentLabelOne, deploymentLabelDBTypes, false, deploymentLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DeploymentLabel struct: %s", err)
	}
	if err = randomize.Struct(seed, deploymentLabelTwo, deploymentLabelDBTypes, false, deploymentLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DeploymentLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = deploymentLabelOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = deploymentLabelTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := DeploymentLabels().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testDeploymentLabelsCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	deploymentLabelOne := &DeploymentLabel{}
	deploymentLabelTwo := &DeploymentLabel{}
	if err = randomize.Struct(seed, deploymentLabelOne, deploymentLabelDBTypes, false, deploymentLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DeploymentLabel struct: %s", err)
	}
	if err = randomize.Struct(seed, deploymentLabelTwo, deploymentLabelDBTypes, false, deploymentLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DeploymentLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = deploymentLabelOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = deploymentLabelTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := DeploymentLabels().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func deploymentLabelBeforeInsertHook(ctx context.Context, e boil.ContextExecutor, o *DeploymentLabel) error {
	*o = DeploymentLabel{}
	return nil
}

func deploymentLabelAfterInsertHook(ctx context.Context, e boil.ContextExecutor, o *DeploymentLabel) error {
	*o = DeploymentLabel{}
	return nil
}

func deploymentLabelAfterSelectHook(ctx context.Context, e boil.ContextExecutor, o *DeploymentLabel) error {
	*o = DeploymentLabel{}
	return nil
}

func deploymentLabelBeforeUpdateHook(ctx context.Context, e boil.ContextExecutor, o *DeploymentLabel) error {
	*o = DeploymentLabel{}
	return nil
}

func deploymentLabelAfterUpdateHook(ctx context.Context, e boil.ContextExecutor, o *DeploymentLabel) error {
	*o = DeploymentLabel{}
	return nil
}

func deploymentLabelBeforeDeleteHook(ctx context.Context, e boil.ContextExecutor, o *DeploymentLabel) error {
	*o = DeploymentLabel{}
	return nil
}

func deploymentLabelAfterDeleteHook(ctx context.Context, e boil.ContextExecutor, o *DeploymentLabel) error {
	*o = DeploymentLabel{}
	return nil
}

func deploymentLabelBeforeUpsertHook(ctx context.Context, e boil.ContextExecutor, o *DeploymentLabel) error {
	*o = DeploymentLabel{}
	return nil
}

func deploymentLabelAfterUpsertHook(ctx context.Context, e boil.ContextExecutor, o *DeploymentLabel) error {
	*o = DeploymentLabel{}
	return nil
}

func testDeploymentLabelsHooks(t *testing.T) {
	t.Parallel()

	var err error

	ctx := context.Background()
	empty := &DeploymentLabel{}
	o := &DeploymentLabel{}

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, o, deploymentLabelDBTypes, false); err != nil {
		t.Errorf("Unable to randomize DeploymentLabel object: %s", err)
	}

	AddDeploymentLabelHook(boil.BeforeInsertHook, deploymentLabelBeforeInsertHook)
	if err = o.doBeforeInsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeInsertHook function to empty object, but got: %#v", o)
	}
	deploymentLabelBeforeInsertHooks = []DeploymentLabelHook{}

	AddDeploymentLabelHook(boil.AfterInsertHook, deploymentLabelAfterInsertHook)
	if err = o.doAfterInsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterInsertHook function to empty object, but got: %#v", o)
	}
	deploymentLabelAfterInsertHooks = []DeploymentLabelHook{}

	AddDeploymentLabelHook(boil.AfterSelectHook, deploymentLabelAfterSelectHook)
	if err = o.doAfterSelectHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterSelectHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterSelectHook function to empty object, but got: %#v", o)
	}
	deploymentLabelAfterSelectHooks = []DeploymentLabelHook{}

	AddDeploymentLabelHook(boil.BeforeUpdateHook, deploymentLabelBeforeUpdateHook)
	if err = o.doBeforeUpdateHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpdateHook function to empty object, but got: %#v", o)
	}
	deploymentLabelBeforeUpdateHooks = []DeploymentLabelHook{}

	AddDeploymentLabelHook(boil.AfterUpdateHook, deploymentLabelAfterUpdateHook)
	if err = o.doAfterUpdateHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpdateHook function to empty object, but got: %#v", o)
	}
	deploymentLabelAfterUpdateHooks = []DeploymentLabelHook{}

	AddDeploymentLabelHook(boil.BeforeDeleteHook, deploymentLabelBeforeDeleteHook)
	if err = o.doBeforeDeleteHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeDeleteHook function to empty object, but got: %#v", o)
	}
	deploymentLabelBeforeDeleteHooks = []DeploymentLabelHook{}

	AddDeploymentLabelHook(boil.AfterDeleteHook, deploymentLabelAfterDeleteHook)
	if err = o.doAfterDeleteHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterDeleteHook function to empty object, but got: %#v", o)
	}
	deploymentLabelAfterDeleteHooks = []DeploymentLabelHook{}

	AddDeploymentLabelHook(boil.BeforeUpsertHook, deploymentLabelBeforeUpsertHook)
	if err = o.doBeforeUpsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpsertHook function to empty object, but got: %#v", o)
	}
	deploymentLabelBeforeUpsertHooks = []DeploymentLabelHook{}

	AddDeploymentLabelHook(boil.AfterUpsertHook, deploymentLabelAfterUpsertHook)
	if err = o.doAfterUpsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpsertHook function to empty object, but got: %#v", o)
	}
	deploymentLabelAfterUpsertHooks = []DeploymentLabelHook{}
}

func testDeploymentLabelsInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DeploymentLabel{}
	if err = randomize.Struct(seed, o, deploymentLabelDBTypes, true, deploymentLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DeploymentLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := DeploymentLabels().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testDeploymentLabelsInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DeploymentLabel{}
	if err = randomize.Struct(seed, o, deploymentLabelDBTypes, true); err != nil {
		t.Errorf("Unable to randomize DeploymentLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Whitelist(deploymentLabelColumnsWithoutDefault...)); err != nil {
		t.Error(err)
	}

	count, err := DeploymentLabels().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testDeploymentLabelToOneDeploymentUsingResource(t *testing.T) {
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var local DeploymentLabel
	var foreign Deployment

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, deploymentLabelDBTypes, true, deploymentLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DeploymentLabel struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, deploymentDBTypes, false, deploymentColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Deployment struct: %s", err)
	}

	if err := foreign.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	queries.Assign(&local.ResourceGUID, foreign.GUID)
	if err := local.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := local.Resource().One(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	if !queries.Equal(check.GUID, foreign.GUID) {
		t.Errorf("want: %v, got %v", foreign.GUID, check.GUID)
	}

	slice := DeploymentLabelSlice{&local}
	if err = local.L.LoadResource(ctx, tx, false, (*[]*DeploymentLabel)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Resource == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.Resource = nil
	if err = local.L.LoadResource(ctx, tx, true, &local, nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Resource == nil {
		t.Error("struct should have been eager loaded")
	}
}

func testDeploymentLabelToOneSetOpDeploymentUsingResource(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a DeploymentLabel
	var b, c Deployment

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, deploymentLabelDBTypes, false, strmangle.SetComplement(deploymentLabelPrimaryKeyColumns, deploymentLabelColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, deploymentDBTypes, false, strmangle.SetComplement(deploymentPrimaryKeyColumns, deploymentColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, deploymentDBTypes, false, strmangle.SetComplement(deploymentPrimaryKeyColumns, deploymentColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*Deployment{&b, &c} {
		err = a.SetResource(ctx, tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.Resource != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.ResourceDeploymentLabels[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if !queries.Equal(a.ResourceGUID, x.GUID) {
			t.Error("foreign key was wrong value", a.ResourceGUID)
		}

		zero := reflect.Zero(reflect.TypeOf(a.ResourceGUID))
		reflect.Indirect(reflect.ValueOf(&a.ResourceGUID)).Set(zero)

		if err = a.Reload(ctx, tx); err != nil {
			t.Fatal("failed to reload", err)
		}

		if !queries.Equal(a.ResourceGUID, x.GUID) {
			t.Error("foreign key was wrong value", a.ResourceGUID, x.GUID)
		}
	}
}

func testDeploymentLabelToOneRemoveOpDeploymentUsingResource(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a DeploymentLabel
	var b Deployment

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, deploymentLabelDBTypes, false, strmangle.SetComplement(deploymentLabelPrimaryKeyColumns, deploymentLabelColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, deploymentDBTypes, false, strmangle.SetComplement(deploymentPrimaryKeyColumns, deploymentColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err = a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	if err = a.SetResource(ctx, tx, true, &b); err != nil {
		t.Fatal(err)
	}

	if err = a.RemoveResource(ctx, tx, &b); err != nil {
		t.Error("failed to remove relationship")
	}

	count, err := a.Resource().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 0 {
		t.Error("want no relationships remaining")
	}

	if a.R.Resource != nil {
		t.Error("R struct entry should be nil")
	}

	if !queries.IsValuerNil(a.ResourceGUID) {
		t.Error("foreign key value should be nil")
	}

	if len(b.R.ResourceDeploymentLabels) != 0 {
		t.Error("failed to remove a from b's relationships")
	}
}

func testDeploymentLabelsReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DeploymentLabel{}
	if err = randomize.Struct(seed, o, deploymentLabelDBTypes, true, deploymentLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DeploymentLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testDeploymentLabelsReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DeploymentLabel{}
	if err = randomize.Struct(seed, o, deploymentLabelDBTypes, true, deploymentLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DeploymentLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := DeploymentLabelSlice{o}

	if err = slice.ReloadAll(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testDeploymentLabelsSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DeploymentLabel{}
	if err = randomize.Struct(seed, o, deploymentLabelDBTypes, true, deploymentLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DeploymentLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := DeploymentLabels().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	deploymentLabelDBTypes = map[string]string{`ID`: `integer`, `GUID`: `text`, `CreatedAt`: `timestamp without time zone`, `UpdatedAt`: `timestamp without time zone`, `ResourceGUID`: `character varying`, `KeyPrefix`: `character varying`, `KeyName`: `character varying`, `Value`: `character varying`}
	_                      = bytes.MinRead
)

func testDeploymentLabelsUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(deploymentLabelPrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(deploymentLabelAllColumns) == len(deploymentLabelPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &DeploymentLabel{}
	if err = randomize.Struct(seed, o, deploymentLabelDBTypes, true, deploymentLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DeploymentLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := DeploymentLabels().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, deploymentLabelDBTypes, true, deploymentLabelPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize DeploymentLabel struct: %s", err)
	}

	if rowsAff, err := o.Update(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testDeploymentLabelsSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(deploymentLabelAllColumns) == len(deploymentLabelPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &DeploymentLabel{}
	if err = randomize.Struct(seed, o, deploymentLabelDBTypes, true, deploymentLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DeploymentLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := DeploymentLabels().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, deploymentLabelDBTypes, true, deploymentLabelPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize DeploymentLabel struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(deploymentLabelAllColumns, deploymentLabelPrimaryKeyColumns) {
		fields = deploymentLabelAllColumns
	} else {
		fields = strmangle.SetComplement(
			deploymentLabelAllColumns,
			deploymentLabelPrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := DeploymentLabelSlice{o}
	if rowsAff, err := slice.UpdateAll(ctx, tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testDeploymentLabelsUpsert(t *testing.T) {
	t.Parallel()

	if len(deploymentLabelAllColumns) == len(deploymentLabelPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := DeploymentLabel{}
	if err = randomize.Struct(seed, &o, deploymentLabelDBTypes, true); err != nil {
		t.Errorf("Unable to randomize DeploymentLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(ctx, tx, false, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert DeploymentLabel: %s", err)
	}

	count, err := DeploymentLabels().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, deploymentLabelDBTypes, false, deploymentLabelPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize DeploymentLabel struct: %s", err)
	}

	if err = o.Upsert(ctx, tx, true, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert DeploymentLabel: %s", err)
	}

	count, err = DeploymentLabels().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
