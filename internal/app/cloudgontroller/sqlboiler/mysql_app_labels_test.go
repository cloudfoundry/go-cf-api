// +build mysql
// Code generated by SQLBoiler 4.6.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"bytes"
	"context"
	"reflect"
	"testing"

	"github.com/volatiletech/randomize"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testAppLabels(t *testing.T) {
	t.Parallel()

	query := AppLabels()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testAppLabelsDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &AppLabel{}
	if err = randomize.Struct(seed, o, appLabelDBTypes, true, appLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize AppLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := AppLabels().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testAppLabelsQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &AppLabel{}
	if err = randomize.Struct(seed, o, appLabelDBTypes, true, appLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize AppLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := AppLabels().DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := AppLabels().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testAppLabelsSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &AppLabel{}
	if err = randomize.Struct(seed, o, appLabelDBTypes, true, appLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize AppLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := AppLabelSlice{o}

	if rowsAff, err := slice.DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := AppLabels().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testAppLabelsExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &AppLabel{}
	if err = randomize.Struct(seed, o, appLabelDBTypes, true, appLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize AppLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := AppLabelExists(ctx, tx, o.ID)
	if err != nil {
		t.Errorf("Unable to check if AppLabel exists: %s", err)
	}
	if !e {
		t.Errorf("Expected AppLabelExists to return true, but got false.")
	}
}

func testAppLabelsFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &AppLabel{}
	if err = randomize.Struct(seed, o, appLabelDBTypes, true, appLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize AppLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	appLabelFound, err := FindAppLabel(ctx, tx, o.ID)
	if err != nil {
		t.Error(err)
	}

	if appLabelFound == nil {
		t.Error("want a record, got nil")
	}
}

func testAppLabelsBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &AppLabel{}
	if err = randomize.Struct(seed, o, appLabelDBTypes, true, appLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize AppLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = AppLabels().Bind(ctx, tx, o); err != nil {
		t.Error(err)
	}
}

func testAppLabelsOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &AppLabel{}
	if err = randomize.Struct(seed, o, appLabelDBTypes, true, appLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize AppLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := AppLabels().One(ctx, tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testAppLabelsAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	appLabelOne := &AppLabel{}
	appLabelTwo := &AppLabel{}
	if err = randomize.Struct(seed, appLabelOne, appLabelDBTypes, false, appLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize AppLabel struct: %s", err)
	}
	if err = randomize.Struct(seed, appLabelTwo, appLabelDBTypes, false, appLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize AppLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = appLabelOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = appLabelTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := AppLabels().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testAppLabelsCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	appLabelOne := &AppLabel{}
	appLabelTwo := &AppLabel{}
	if err = randomize.Struct(seed, appLabelOne, appLabelDBTypes, false, appLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize AppLabel struct: %s", err)
	}
	if err = randomize.Struct(seed, appLabelTwo, appLabelDBTypes, false, appLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize AppLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = appLabelOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = appLabelTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := AppLabels().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func appLabelBeforeInsertHook(ctx context.Context, e boil.ContextExecutor, o *AppLabel) error {
	*o = AppLabel{}
	return nil
}

func appLabelAfterInsertHook(ctx context.Context, e boil.ContextExecutor, o *AppLabel) error {
	*o = AppLabel{}
	return nil
}

func appLabelAfterSelectHook(ctx context.Context, e boil.ContextExecutor, o *AppLabel) error {
	*o = AppLabel{}
	return nil
}

func appLabelBeforeUpdateHook(ctx context.Context, e boil.ContextExecutor, o *AppLabel) error {
	*o = AppLabel{}
	return nil
}

func appLabelAfterUpdateHook(ctx context.Context, e boil.ContextExecutor, o *AppLabel) error {
	*o = AppLabel{}
	return nil
}

func appLabelBeforeDeleteHook(ctx context.Context, e boil.ContextExecutor, o *AppLabel) error {
	*o = AppLabel{}
	return nil
}

func appLabelAfterDeleteHook(ctx context.Context, e boil.ContextExecutor, o *AppLabel) error {
	*o = AppLabel{}
	return nil
}

func appLabelBeforeUpsertHook(ctx context.Context, e boil.ContextExecutor, o *AppLabel) error {
	*o = AppLabel{}
	return nil
}

func appLabelAfterUpsertHook(ctx context.Context, e boil.ContextExecutor, o *AppLabel) error {
	*o = AppLabel{}
	return nil
}

func testAppLabelsHooks(t *testing.T) {
	t.Parallel()

	var err error

	ctx := context.Background()
	empty := &AppLabel{}
	o := &AppLabel{}

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, o, appLabelDBTypes, false); err != nil {
		t.Errorf("Unable to randomize AppLabel object: %s", err)
	}

	AddAppLabelHook(boil.BeforeInsertHook, appLabelBeforeInsertHook)
	if err = o.doBeforeInsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeInsertHook function to empty object, but got: %#v", o)
	}
	appLabelBeforeInsertHooks = []AppLabelHook{}

	AddAppLabelHook(boil.AfterInsertHook, appLabelAfterInsertHook)
	if err = o.doAfterInsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterInsertHook function to empty object, but got: %#v", o)
	}
	appLabelAfterInsertHooks = []AppLabelHook{}

	AddAppLabelHook(boil.AfterSelectHook, appLabelAfterSelectHook)
	if err = o.doAfterSelectHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterSelectHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterSelectHook function to empty object, but got: %#v", o)
	}
	appLabelAfterSelectHooks = []AppLabelHook{}

	AddAppLabelHook(boil.BeforeUpdateHook, appLabelBeforeUpdateHook)
	if err = o.doBeforeUpdateHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpdateHook function to empty object, but got: %#v", o)
	}
	appLabelBeforeUpdateHooks = []AppLabelHook{}

	AddAppLabelHook(boil.AfterUpdateHook, appLabelAfterUpdateHook)
	if err = o.doAfterUpdateHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpdateHook function to empty object, but got: %#v", o)
	}
	appLabelAfterUpdateHooks = []AppLabelHook{}

	AddAppLabelHook(boil.BeforeDeleteHook, appLabelBeforeDeleteHook)
	if err = o.doBeforeDeleteHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeDeleteHook function to empty object, but got: %#v", o)
	}
	appLabelBeforeDeleteHooks = []AppLabelHook{}

	AddAppLabelHook(boil.AfterDeleteHook, appLabelAfterDeleteHook)
	if err = o.doAfterDeleteHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterDeleteHook function to empty object, but got: %#v", o)
	}
	appLabelAfterDeleteHooks = []AppLabelHook{}

	AddAppLabelHook(boil.BeforeUpsertHook, appLabelBeforeUpsertHook)
	if err = o.doBeforeUpsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpsertHook function to empty object, but got: %#v", o)
	}
	appLabelBeforeUpsertHooks = []AppLabelHook{}

	AddAppLabelHook(boil.AfterUpsertHook, appLabelAfterUpsertHook)
	if err = o.doAfterUpsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpsertHook function to empty object, but got: %#v", o)
	}
	appLabelAfterUpsertHooks = []AppLabelHook{}
}

func testAppLabelsInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &AppLabel{}
	if err = randomize.Struct(seed, o, appLabelDBTypes, true, appLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize AppLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := AppLabels().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testAppLabelsInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &AppLabel{}
	if err = randomize.Struct(seed, o, appLabelDBTypes, true); err != nil {
		t.Errorf("Unable to randomize AppLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Whitelist(appLabelColumnsWithoutDefault...)); err != nil {
		t.Error(err)
	}

	count, err := AppLabels().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testAppLabelToOneAppUsingResource(t *testing.T) {
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var local AppLabel
	var foreign App

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, appLabelDBTypes, true, appLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize AppLabel struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, appDBTypes, false, appColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize App struct: %s", err)
	}

	if err := foreign.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	queries.Assign(&local.ResourceGUID, foreign.GUID)
	if err := local.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := local.Resource().One(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	if !queries.Equal(check.GUID, foreign.GUID) {
		t.Errorf("want: %v, got %v", foreign.GUID, check.GUID)
	}

	slice := AppLabelSlice{&local}
	if err = local.L.LoadResource(ctx, tx, false, (*[]*AppLabel)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Resource == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.Resource = nil
	if err = local.L.LoadResource(ctx, tx, true, &local, nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Resource == nil {
		t.Error("struct should have been eager loaded")
	}
}

func testAppLabelToOneSetOpAppUsingResource(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a AppLabel
	var b, c App

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, appLabelDBTypes, false, strmangle.SetComplement(appLabelPrimaryKeyColumns, appLabelColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, appDBTypes, false, strmangle.SetComplement(appPrimaryKeyColumns, appColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, appDBTypes, false, strmangle.SetComplement(appPrimaryKeyColumns, appColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*App{&b, &c} {
		err = a.SetResource(ctx, tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.Resource != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.ResourceAppLabels[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if !queries.Equal(a.ResourceGUID, x.GUID) {
			t.Error("foreign key was wrong value", a.ResourceGUID)
		}

		zero := reflect.Zero(reflect.TypeOf(a.ResourceGUID))
		reflect.Indirect(reflect.ValueOf(&a.ResourceGUID)).Set(zero)

		if err = a.Reload(ctx, tx); err != nil {
			t.Fatal("failed to reload", err)
		}

		if !queries.Equal(a.ResourceGUID, x.GUID) {
			t.Error("foreign key was wrong value", a.ResourceGUID, x.GUID)
		}
	}
}

func testAppLabelToOneRemoveOpAppUsingResource(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a AppLabel
	var b App

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, appLabelDBTypes, false, strmangle.SetComplement(appLabelPrimaryKeyColumns, appLabelColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, appDBTypes, false, strmangle.SetComplement(appPrimaryKeyColumns, appColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err = a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	if err = a.SetResource(ctx, tx, true, &b); err != nil {
		t.Fatal(err)
	}

	if err = a.RemoveResource(ctx, tx, &b); err != nil {
		t.Error("failed to remove relationship")
	}

	count, err := a.Resource().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 0 {
		t.Error("want no relationships remaining")
	}

	if a.R.Resource != nil {
		t.Error("R struct entry should be nil")
	}

	if !queries.IsValuerNil(a.ResourceGUID) {
		t.Error("foreign key value should be nil")
	}

	if len(b.R.ResourceAppLabels) != 0 {
		t.Error("failed to remove a from b's relationships")
	}
}

func testAppLabelsReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &AppLabel{}
	if err = randomize.Struct(seed, o, appLabelDBTypes, true, appLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize AppLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testAppLabelsReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &AppLabel{}
	if err = randomize.Struct(seed, o, appLabelDBTypes, true, appLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize AppLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := AppLabelSlice{o}

	if err = slice.ReloadAll(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testAppLabelsSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &AppLabel{}
	if err = randomize.Struct(seed, o, appLabelDBTypes, true, appLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize AppLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := AppLabels().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	appLabelDBTypes = map[string]string{`ID`: `int`, `GUID`: `varchar`, `CreatedAt`: `timestamp`, `UpdatedAt`: `timestamp`, `ResourceGUID`: `varchar`, `KeyPrefix`: `varchar`, `KeyName`: `varchar`, `Value`: `varchar`}
	_               = bytes.MinRead
)

func testAppLabelsUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(appLabelPrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(appLabelAllColumns) == len(appLabelPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &AppLabel{}
	if err = randomize.Struct(seed, o, appLabelDBTypes, true, appLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize AppLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := AppLabels().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, appLabelDBTypes, true, appLabelPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize AppLabel struct: %s", err)
	}

	if rowsAff, err := o.Update(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testAppLabelsSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(appLabelAllColumns) == len(appLabelPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &AppLabel{}
	if err = randomize.Struct(seed, o, appLabelDBTypes, true, appLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize AppLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := AppLabels().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, appLabelDBTypes, true, appLabelPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize AppLabel struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(appLabelAllColumns, appLabelPrimaryKeyColumns) {
		fields = appLabelAllColumns
	} else {
		fields = strmangle.SetComplement(
			appLabelAllColumns,
			appLabelPrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := AppLabelSlice{o}
	if rowsAff, err := slice.UpdateAll(ctx, tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testAppLabelsUpsert(t *testing.T) {
	t.Parallel()

	if len(appLabelAllColumns) == len(appLabelPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}
	if len(mySQLAppLabelUniqueColumns) == 0 {
		t.Skip("Skipping table with no unique columns to conflict on")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := AppLabel{}
	if err = randomize.Struct(seed, &o, appLabelDBTypes, false); err != nil {
		t.Errorf("Unable to randomize AppLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(ctx, tx, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert AppLabel: %s", err)
	}

	count, err := AppLabels().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, appLabelDBTypes, false, appLabelPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize AppLabel struct: %s", err)
	}

	if err = o.Upsert(ctx, tx, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert AppLabel: %s", err)
	}

	count, err = AppLabels().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
