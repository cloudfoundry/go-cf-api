// +build mysql_integration
// Code generated by SQLBoiler 4.6.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"bytes"
	"context"
	"reflect"
	"testing"

	"github.com/volatiletech/randomize"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testBuildLabels(t *testing.T) {
	t.Parallel()

	query := BuildLabels()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testBuildLabelsDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &BuildLabel{}
	if err = randomize.Struct(seed, o, buildLabelDBTypes, true, buildLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize BuildLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := BuildLabels().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testBuildLabelsQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &BuildLabel{}
	if err = randomize.Struct(seed, o, buildLabelDBTypes, true, buildLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize BuildLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := BuildLabels().DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := BuildLabels().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testBuildLabelsSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &BuildLabel{}
	if err = randomize.Struct(seed, o, buildLabelDBTypes, true, buildLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize BuildLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := BuildLabelSlice{o}

	if rowsAff, err := slice.DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := BuildLabels().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testBuildLabelsExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &BuildLabel{}
	if err = randomize.Struct(seed, o, buildLabelDBTypes, true, buildLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize BuildLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := BuildLabelExists(ctx, tx, o.ID)
	if err != nil {
		t.Errorf("Unable to check if BuildLabel exists: %s", err)
	}
	if !e {
		t.Errorf("Expected BuildLabelExists to return true, but got false.")
	}
}

func testBuildLabelsFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &BuildLabel{}
	if err = randomize.Struct(seed, o, buildLabelDBTypes, true, buildLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize BuildLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	buildLabelFound, err := FindBuildLabel(ctx, tx, o.ID)
	if err != nil {
		t.Error(err)
	}

	if buildLabelFound == nil {
		t.Error("want a record, got nil")
	}
}

func testBuildLabelsBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &BuildLabel{}
	if err = randomize.Struct(seed, o, buildLabelDBTypes, true, buildLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize BuildLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = BuildLabels().Bind(ctx, tx, o); err != nil {
		t.Error(err)
	}
}

func testBuildLabelsOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &BuildLabel{}
	if err = randomize.Struct(seed, o, buildLabelDBTypes, true, buildLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize BuildLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := BuildLabels().One(ctx, tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testBuildLabelsAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	buildLabelOne := &BuildLabel{}
	buildLabelTwo := &BuildLabel{}
	if err = randomize.Struct(seed, buildLabelOne, buildLabelDBTypes, false, buildLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize BuildLabel struct: %s", err)
	}
	if err = randomize.Struct(seed, buildLabelTwo, buildLabelDBTypes, false, buildLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize BuildLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = buildLabelOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = buildLabelTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := BuildLabels().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testBuildLabelsCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	buildLabelOne := &BuildLabel{}
	buildLabelTwo := &BuildLabel{}
	if err = randomize.Struct(seed, buildLabelOne, buildLabelDBTypes, false, buildLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize BuildLabel struct: %s", err)
	}
	if err = randomize.Struct(seed, buildLabelTwo, buildLabelDBTypes, false, buildLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize BuildLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = buildLabelOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = buildLabelTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := BuildLabels().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func buildLabelBeforeInsertHook(ctx context.Context, e boil.ContextExecutor, o *BuildLabel) error {
	*o = BuildLabel{}
	return nil
}

func buildLabelAfterInsertHook(ctx context.Context, e boil.ContextExecutor, o *BuildLabel) error {
	*o = BuildLabel{}
	return nil
}

func buildLabelAfterSelectHook(ctx context.Context, e boil.ContextExecutor, o *BuildLabel) error {
	*o = BuildLabel{}
	return nil
}

func buildLabelBeforeUpdateHook(ctx context.Context, e boil.ContextExecutor, o *BuildLabel) error {
	*o = BuildLabel{}
	return nil
}

func buildLabelAfterUpdateHook(ctx context.Context, e boil.ContextExecutor, o *BuildLabel) error {
	*o = BuildLabel{}
	return nil
}

func buildLabelBeforeDeleteHook(ctx context.Context, e boil.ContextExecutor, o *BuildLabel) error {
	*o = BuildLabel{}
	return nil
}

func buildLabelAfterDeleteHook(ctx context.Context, e boil.ContextExecutor, o *BuildLabel) error {
	*o = BuildLabel{}
	return nil
}

func buildLabelBeforeUpsertHook(ctx context.Context, e boil.ContextExecutor, o *BuildLabel) error {
	*o = BuildLabel{}
	return nil
}

func buildLabelAfterUpsertHook(ctx context.Context, e boil.ContextExecutor, o *BuildLabel) error {
	*o = BuildLabel{}
	return nil
}

func testBuildLabelsHooks(t *testing.T) {
	t.Parallel()

	var err error

	ctx := context.Background()
	empty := &BuildLabel{}
	o := &BuildLabel{}

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, o, buildLabelDBTypes, false); err != nil {
		t.Errorf("Unable to randomize BuildLabel object: %s", err)
	}

	AddBuildLabelHook(boil.BeforeInsertHook, buildLabelBeforeInsertHook)
	if err = o.doBeforeInsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeInsertHook function to empty object, but got: %#v", o)
	}
	buildLabelBeforeInsertHooks = []BuildLabelHook{}

	AddBuildLabelHook(boil.AfterInsertHook, buildLabelAfterInsertHook)
	if err = o.doAfterInsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterInsertHook function to empty object, but got: %#v", o)
	}
	buildLabelAfterInsertHooks = []BuildLabelHook{}

	AddBuildLabelHook(boil.AfterSelectHook, buildLabelAfterSelectHook)
	if err = o.doAfterSelectHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterSelectHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterSelectHook function to empty object, but got: %#v", o)
	}
	buildLabelAfterSelectHooks = []BuildLabelHook{}

	AddBuildLabelHook(boil.BeforeUpdateHook, buildLabelBeforeUpdateHook)
	if err = o.doBeforeUpdateHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpdateHook function to empty object, but got: %#v", o)
	}
	buildLabelBeforeUpdateHooks = []BuildLabelHook{}

	AddBuildLabelHook(boil.AfterUpdateHook, buildLabelAfterUpdateHook)
	if err = o.doAfterUpdateHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpdateHook function to empty object, but got: %#v", o)
	}
	buildLabelAfterUpdateHooks = []BuildLabelHook{}

	AddBuildLabelHook(boil.BeforeDeleteHook, buildLabelBeforeDeleteHook)
	if err = o.doBeforeDeleteHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeDeleteHook function to empty object, but got: %#v", o)
	}
	buildLabelBeforeDeleteHooks = []BuildLabelHook{}

	AddBuildLabelHook(boil.AfterDeleteHook, buildLabelAfterDeleteHook)
	if err = o.doAfterDeleteHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterDeleteHook function to empty object, but got: %#v", o)
	}
	buildLabelAfterDeleteHooks = []BuildLabelHook{}

	AddBuildLabelHook(boil.BeforeUpsertHook, buildLabelBeforeUpsertHook)
	if err = o.doBeforeUpsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpsertHook function to empty object, but got: %#v", o)
	}
	buildLabelBeforeUpsertHooks = []BuildLabelHook{}

	AddBuildLabelHook(boil.AfterUpsertHook, buildLabelAfterUpsertHook)
	if err = o.doAfterUpsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpsertHook function to empty object, but got: %#v", o)
	}
	buildLabelAfterUpsertHooks = []BuildLabelHook{}
}

func testBuildLabelsInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &BuildLabel{}
	if err = randomize.Struct(seed, o, buildLabelDBTypes, true, buildLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize BuildLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := BuildLabels().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testBuildLabelsInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &BuildLabel{}
	if err = randomize.Struct(seed, o, buildLabelDBTypes, true); err != nil {
		t.Errorf("Unable to randomize BuildLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Whitelist(buildLabelColumnsWithoutDefault...)); err != nil {
		t.Error(err)
	}

	count, err := BuildLabels().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testBuildLabelToOneBuildUsingResource(t *testing.T) {
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var local BuildLabel
	var foreign Build

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, buildLabelDBTypes, true, buildLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize BuildLabel struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, buildDBTypes, false, buildColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Build struct: %s", err)
	}

	if err := foreign.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	queries.Assign(&local.ResourceGUID, foreign.GUID)
	if err := local.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := local.Resource().One(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	if !queries.Equal(check.GUID, foreign.GUID) {
		t.Errorf("want: %v, got %v", foreign.GUID, check.GUID)
	}

	slice := BuildLabelSlice{&local}
	if err = local.L.LoadResource(ctx, tx, false, (*[]*BuildLabel)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Resource == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.Resource = nil
	if err = local.L.LoadResource(ctx, tx, true, &local, nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Resource == nil {
		t.Error("struct should have been eager loaded")
	}
}

func testBuildLabelToOneSetOpBuildUsingResource(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a BuildLabel
	var b, c Build

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, buildLabelDBTypes, false, strmangle.SetComplement(buildLabelPrimaryKeyColumns, buildLabelColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, buildDBTypes, false, strmangle.SetComplement(buildPrimaryKeyColumns, buildColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, buildDBTypes, false, strmangle.SetComplement(buildPrimaryKeyColumns, buildColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*Build{&b, &c} {
		err = a.SetResource(ctx, tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.Resource != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.ResourceBuildLabels[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if !queries.Equal(a.ResourceGUID, x.GUID) {
			t.Error("foreign key was wrong value", a.ResourceGUID)
		}

		zero := reflect.Zero(reflect.TypeOf(a.ResourceGUID))
		reflect.Indirect(reflect.ValueOf(&a.ResourceGUID)).Set(zero)

		if err = a.Reload(ctx, tx); err != nil {
			t.Fatal("failed to reload", err)
		}

		if !queries.Equal(a.ResourceGUID, x.GUID) {
			t.Error("foreign key was wrong value", a.ResourceGUID, x.GUID)
		}
	}
}

func testBuildLabelToOneRemoveOpBuildUsingResource(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a BuildLabel
	var b Build

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, buildLabelDBTypes, false, strmangle.SetComplement(buildLabelPrimaryKeyColumns, buildLabelColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, buildDBTypes, false, strmangle.SetComplement(buildPrimaryKeyColumns, buildColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err = a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	if err = a.SetResource(ctx, tx, true, &b); err != nil {
		t.Fatal(err)
	}

	if err = a.RemoveResource(ctx, tx, &b); err != nil {
		t.Error("failed to remove relationship")
	}

	count, err := a.Resource().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 0 {
		t.Error("want no relationships remaining")
	}

	if a.R.Resource != nil {
		t.Error("R struct entry should be nil")
	}

	if !queries.IsValuerNil(a.ResourceGUID) {
		t.Error("foreign key value should be nil")
	}

	if len(b.R.ResourceBuildLabels) != 0 {
		t.Error("failed to remove a from b's relationships")
	}
}

func testBuildLabelsReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &BuildLabel{}
	if err = randomize.Struct(seed, o, buildLabelDBTypes, true, buildLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize BuildLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testBuildLabelsReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &BuildLabel{}
	if err = randomize.Struct(seed, o, buildLabelDBTypes, true, buildLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize BuildLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := BuildLabelSlice{o}

	if err = slice.ReloadAll(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testBuildLabelsSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &BuildLabel{}
	if err = randomize.Struct(seed, o, buildLabelDBTypes, true, buildLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize BuildLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := BuildLabels().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	buildLabelDBTypes = map[string]string{`ID`: `int`, `GUID`: `varchar`, `CreatedAt`: `timestamp`, `UpdatedAt`: `timestamp`, `ResourceGUID`: `varchar`, `KeyPrefix`: `varchar`, `KeyName`: `varchar`, `Value`: `varchar`}
	_                 = bytes.MinRead
)

func testBuildLabelsUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(buildLabelPrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(buildLabelAllColumns) == len(buildLabelPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &BuildLabel{}
	if err = randomize.Struct(seed, o, buildLabelDBTypes, true, buildLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize BuildLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := BuildLabels().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, buildLabelDBTypes, true, buildLabelPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize BuildLabel struct: %s", err)
	}

	if rowsAff, err := o.Update(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testBuildLabelsSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(buildLabelAllColumns) == len(buildLabelPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &BuildLabel{}
	if err = randomize.Struct(seed, o, buildLabelDBTypes, true, buildLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize BuildLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := BuildLabels().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, buildLabelDBTypes, true, buildLabelPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize BuildLabel struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(buildLabelAllColumns, buildLabelPrimaryKeyColumns) {
		fields = buildLabelAllColumns
	} else {
		fields = strmangle.SetComplement(
			buildLabelAllColumns,
			buildLabelPrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := BuildLabelSlice{o}
	if rowsAff, err := slice.UpdateAll(ctx, tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testBuildLabelsUpsert(t *testing.T) {
	t.Parallel()

	if len(buildLabelAllColumns) == len(buildLabelPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}
	if len(mySQLBuildLabelUniqueColumns) == 0 {
		t.Skip("Skipping table with no unique columns to conflict on")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := BuildLabel{}
	if err = randomize.Struct(seed, &o, buildLabelDBTypes, false); err != nil {
		t.Errorf("Unable to randomize BuildLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(ctx, tx, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert BuildLabel: %s", err)
	}

	count, err := BuildLabels().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, buildLabelDBTypes, false, buildLabelPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize BuildLabel struct: %s", err)
	}

	if err = o.Upsert(ctx, tx, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert BuildLabel: %s", err)
	}

	count, err = BuildLabels().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
