// +build integration
// Code generated by SQLBoiler 4.6.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"bytes"
	"context"
	"reflect"
	"testing"

	"github.com/volatiletech/randomize"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testSpaceAnnotations(t *testing.T) {
	t.Parallel()

	query := SpaceAnnotations()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testSpaceAnnotationsDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SpaceAnnotation{}
	if err = randomize.Struct(seed, o, spaceAnnotationDBTypes, true, spaceAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SpaceAnnotation struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := SpaceAnnotations().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testSpaceAnnotationsQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SpaceAnnotation{}
	if err = randomize.Struct(seed, o, spaceAnnotationDBTypes, true, spaceAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SpaceAnnotation struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := SpaceAnnotations().DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := SpaceAnnotations().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testSpaceAnnotationsSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SpaceAnnotation{}
	if err = randomize.Struct(seed, o, spaceAnnotationDBTypes, true, spaceAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SpaceAnnotation struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := SpaceAnnotationSlice{o}

	if rowsAff, err := slice.DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := SpaceAnnotations().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testSpaceAnnotationsExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SpaceAnnotation{}
	if err = randomize.Struct(seed, o, spaceAnnotationDBTypes, true, spaceAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SpaceAnnotation struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := SpaceAnnotationExists(ctx, tx, o.ID)
	if err != nil {
		t.Errorf("Unable to check if SpaceAnnotation exists: %s", err)
	}
	if !e {
		t.Errorf("Expected SpaceAnnotationExists to return true, but got false.")
	}
}

func testSpaceAnnotationsFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SpaceAnnotation{}
	if err = randomize.Struct(seed, o, spaceAnnotationDBTypes, true, spaceAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SpaceAnnotation struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	spaceAnnotationFound, err := FindSpaceAnnotation(ctx, tx, o.ID)
	if err != nil {
		t.Error(err)
	}

	if spaceAnnotationFound == nil {
		t.Error("want a record, got nil")
	}
}

func testSpaceAnnotationsBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SpaceAnnotation{}
	if err = randomize.Struct(seed, o, spaceAnnotationDBTypes, true, spaceAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SpaceAnnotation struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = SpaceAnnotations().Bind(ctx, tx, o); err != nil {
		t.Error(err)
	}
}

func testSpaceAnnotationsOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SpaceAnnotation{}
	if err = randomize.Struct(seed, o, spaceAnnotationDBTypes, true, spaceAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SpaceAnnotation struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := SpaceAnnotations().One(ctx, tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testSpaceAnnotationsAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	spaceAnnotationOne := &SpaceAnnotation{}
	spaceAnnotationTwo := &SpaceAnnotation{}
	if err = randomize.Struct(seed, spaceAnnotationOne, spaceAnnotationDBTypes, false, spaceAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SpaceAnnotation struct: %s", err)
	}
	if err = randomize.Struct(seed, spaceAnnotationTwo, spaceAnnotationDBTypes, false, spaceAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SpaceAnnotation struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = spaceAnnotationOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = spaceAnnotationTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := SpaceAnnotations().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testSpaceAnnotationsCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	spaceAnnotationOne := &SpaceAnnotation{}
	spaceAnnotationTwo := &SpaceAnnotation{}
	if err = randomize.Struct(seed, spaceAnnotationOne, spaceAnnotationDBTypes, false, spaceAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SpaceAnnotation struct: %s", err)
	}
	if err = randomize.Struct(seed, spaceAnnotationTwo, spaceAnnotationDBTypes, false, spaceAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SpaceAnnotation struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = spaceAnnotationOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = spaceAnnotationTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := SpaceAnnotations().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func spaceAnnotationBeforeInsertHook(ctx context.Context, e boil.ContextExecutor, o *SpaceAnnotation) error {
	*o = SpaceAnnotation{}
	return nil
}

func spaceAnnotationAfterInsertHook(ctx context.Context, e boil.ContextExecutor, o *SpaceAnnotation) error {
	*o = SpaceAnnotation{}
	return nil
}

func spaceAnnotationAfterSelectHook(ctx context.Context, e boil.ContextExecutor, o *SpaceAnnotation) error {
	*o = SpaceAnnotation{}
	return nil
}

func spaceAnnotationBeforeUpdateHook(ctx context.Context, e boil.ContextExecutor, o *SpaceAnnotation) error {
	*o = SpaceAnnotation{}
	return nil
}

func spaceAnnotationAfterUpdateHook(ctx context.Context, e boil.ContextExecutor, o *SpaceAnnotation) error {
	*o = SpaceAnnotation{}
	return nil
}

func spaceAnnotationBeforeDeleteHook(ctx context.Context, e boil.ContextExecutor, o *SpaceAnnotation) error {
	*o = SpaceAnnotation{}
	return nil
}

func spaceAnnotationAfterDeleteHook(ctx context.Context, e boil.ContextExecutor, o *SpaceAnnotation) error {
	*o = SpaceAnnotation{}
	return nil
}

func spaceAnnotationBeforeUpsertHook(ctx context.Context, e boil.ContextExecutor, o *SpaceAnnotation) error {
	*o = SpaceAnnotation{}
	return nil
}

func spaceAnnotationAfterUpsertHook(ctx context.Context, e boil.ContextExecutor, o *SpaceAnnotation) error {
	*o = SpaceAnnotation{}
	return nil
}

func testSpaceAnnotationsHooks(t *testing.T) {
	t.Parallel()

	var err error

	ctx := context.Background()
	empty := &SpaceAnnotation{}
	o := &SpaceAnnotation{}

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, o, spaceAnnotationDBTypes, false); err != nil {
		t.Errorf("Unable to randomize SpaceAnnotation object: %s", err)
	}

	AddSpaceAnnotationHook(boil.BeforeInsertHook, spaceAnnotationBeforeInsertHook)
	if err = o.doBeforeInsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeInsertHook function to empty object, but got: %#v", o)
	}
	spaceAnnotationBeforeInsertHooks = []SpaceAnnotationHook{}

	AddSpaceAnnotationHook(boil.AfterInsertHook, spaceAnnotationAfterInsertHook)
	if err = o.doAfterInsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterInsertHook function to empty object, but got: %#v", o)
	}
	spaceAnnotationAfterInsertHooks = []SpaceAnnotationHook{}

	AddSpaceAnnotationHook(boil.AfterSelectHook, spaceAnnotationAfterSelectHook)
	if err = o.doAfterSelectHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterSelectHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterSelectHook function to empty object, but got: %#v", o)
	}
	spaceAnnotationAfterSelectHooks = []SpaceAnnotationHook{}

	AddSpaceAnnotationHook(boil.BeforeUpdateHook, spaceAnnotationBeforeUpdateHook)
	if err = o.doBeforeUpdateHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpdateHook function to empty object, but got: %#v", o)
	}
	spaceAnnotationBeforeUpdateHooks = []SpaceAnnotationHook{}

	AddSpaceAnnotationHook(boil.AfterUpdateHook, spaceAnnotationAfterUpdateHook)
	if err = o.doAfterUpdateHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpdateHook function to empty object, but got: %#v", o)
	}
	spaceAnnotationAfterUpdateHooks = []SpaceAnnotationHook{}

	AddSpaceAnnotationHook(boil.BeforeDeleteHook, spaceAnnotationBeforeDeleteHook)
	if err = o.doBeforeDeleteHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeDeleteHook function to empty object, but got: %#v", o)
	}
	spaceAnnotationBeforeDeleteHooks = []SpaceAnnotationHook{}

	AddSpaceAnnotationHook(boil.AfterDeleteHook, spaceAnnotationAfterDeleteHook)
	if err = o.doAfterDeleteHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterDeleteHook function to empty object, but got: %#v", o)
	}
	spaceAnnotationAfterDeleteHooks = []SpaceAnnotationHook{}

	AddSpaceAnnotationHook(boil.BeforeUpsertHook, spaceAnnotationBeforeUpsertHook)
	if err = o.doBeforeUpsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpsertHook function to empty object, but got: %#v", o)
	}
	spaceAnnotationBeforeUpsertHooks = []SpaceAnnotationHook{}

	AddSpaceAnnotationHook(boil.AfterUpsertHook, spaceAnnotationAfterUpsertHook)
	if err = o.doAfterUpsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpsertHook function to empty object, but got: %#v", o)
	}
	spaceAnnotationAfterUpsertHooks = []SpaceAnnotationHook{}
}

func testSpaceAnnotationsInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SpaceAnnotation{}
	if err = randomize.Struct(seed, o, spaceAnnotationDBTypes, true, spaceAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SpaceAnnotation struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := SpaceAnnotations().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testSpaceAnnotationsInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SpaceAnnotation{}
	if err = randomize.Struct(seed, o, spaceAnnotationDBTypes, true); err != nil {
		t.Errorf("Unable to randomize SpaceAnnotation struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Whitelist(spaceAnnotationColumnsWithoutDefault...)); err != nil {
		t.Error(err)
	}

	count, err := SpaceAnnotations().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testSpaceAnnotationToOneSpaceUsingResource(t *testing.T) {
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var local SpaceAnnotation
	var foreign Space

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, spaceAnnotationDBTypes, true, spaceAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SpaceAnnotation struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, spaceDBTypes, false, spaceColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Space struct: %s", err)
	}

	if err := foreign.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	queries.Assign(&local.ResourceGUID, foreign.GUID)
	if err := local.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := local.Resource().One(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	if !queries.Equal(check.GUID, foreign.GUID) {
		t.Errorf("want: %v, got %v", foreign.GUID, check.GUID)
	}

	slice := SpaceAnnotationSlice{&local}
	if err = local.L.LoadResource(ctx, tx, false, (*[]*SpaceAnnotation)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Resource == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.Resource = nil
	if err = local.L.LoadResource(ctx, tx, true, &local, nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Resource == nil {
		t.Error("struct should have been eager loaded")
	}
}

func testSpaceAnnotationToOneSetOpSpaceUsingResource(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a SpaceAnnotation
	var b, c Space

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, spaceAnnotationDBTypes, false, strmangle.SetComplement(spaceAnnotationPrimaryKeyColumns, spaceAnnotationColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, spaceDBTypes, false, strmangle.SetComplement(spacePrimaryKeyColumns, spaceColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, spaceDBTypes, false, strmangle.SetComplement(spacePrimaryKeyColumns, spaceColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*Space{&b, &c} {
		err = a.SetResource(ctx, tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.Resource != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.ResourceSpaceAnnotations[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if !queries.Equal(a.ResourceGUID, x.GUID) {
			t.Error("foreign key was wrong value", a.ResourceGUID)
		}

		zero := reflect.Zero(reflect.TypeOf(a.ResourceGUID))
		reflect.Indirect(reflect.ValueOf(&a.ResourceGUID)).Set(zero)

		if err = a.Reload(ctx, tx); err != nil {
			t.Fatal("failed to reload", err)
		}

		if !queries.Equal(a.ResourceGUID, x.GUID) {
			t.Error("foreign key was wrong value", a.ResourceGUID, x.GUID)
		}
	}
}

func testSpaceAnnotationToOneRemoveOpSpaceUsingResource(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a SpaceAnnotation
	var b Space

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, spaceAnnotationDBTypes, false, strmangle.SetComplement(spaceAnnotationPrimaryKeyColumns, spaceAnnotationColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, spaceDBTypes, false, strmangle.SetComplement(spacePrimaryKeyColumns, spaceColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err = a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	if err = a.SetResource(ctx, tx, true, &b); err != nil {
		t.Fatal(err)
	}

	if err = a.RemoveResource(ctx, tx, &b); err != nil {
		t.Error("failed to remove relationship")
	}

	count, err := a.Resource().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 0 {
		t.Error("want no relationships remaining")
	}

	if a.R.Resource != nil {
		t.Error("R struct entry should be nil")
	}

	if !queries.IsValuerNil(a.ResourceGUID) {
		t.Error("foreign key value should be nil")
	}

	if len(b.R.ResourceSpaceAnnotations) != 0 {
		t.Error("failed to remove a from b's relationships")
	}
}

func testSpaceAnnotationsReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SpaceAnnotation{}
	if err = randomize.Struct(seed, o, spaceAnnotationDBTypes, true, spaceAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SpaceAnnotation struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testSpaceAnnotationsReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SpaceAnnotation{}
	if err = randomize.Struct(seed, o, spaceAnnotationDBTypes, true, spaceAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SpaceAnnotation struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := SpaceAnnotationSlice{o}

	if err = slice.ReloadAll(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testSpaceAnnotationsSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &SpaceAnnotation{}
	if err = randomize.Struct(seed, o, spaceAnnotationDBTypes, true, spaceAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SpaceAnnotation struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := SpaceAnnotations().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	spaceAnnotationDBTypes = map[string]string{`ID`: `integer`, `GUID`: `text`, `CreatedAt`: `timestamp without time zone`, `UpdatedAt`: `timestamp without time zone`, `ResourceGUID`: `character varying`, `KeyPrefix`: `character varying`, `Key`: `character varying`, `Value`: `character varying`}
	_                      = bytes.MinRead
)

func testSpaceAnnotationsUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(spaceAnnotationPrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(spaceAnnotationAllColumns) == len(spaceAnnotationPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &SpaceAnnotation{}
	if err = randomize.Struct(seed, o, spaceAnnotationDBTypes, true, spaceAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SpaceAnnotation struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := SpaceAnnotations().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, spaceAnnotationDBTypes, true, spaceAnnotationPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize SpaceAnnotation struct: %s", err)
	}

	if rowsAff, err := o.Update(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testSpaceAnnotationsSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(spaceAnnotationAllColumns) == len(spaceAnnotationPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &SpaceAnnotation{}
	if err = randomize.Struct(seed, o, spaceAnnotationDBTypes, true, spaceAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize SpaceAnnotation struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := SpaceAnnotations().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, spaceAnnotationDBTypes, true, spaceAnnotationPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize SpaceAnnotation struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(spaceAnnotationAllColumns, spaceAnnotationPrimaryKeyColumns) {
		fields = spaceAnnotationAllColumns
	} else {
		fields = strmangle.SetComplement(
			spaceAnnotationAllColumns,
			spaceAnnotationPrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := SpaceAnnotationSlice{o}
	if rowsAff, err := slice.UpdateAll(ctx, tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testSpaceAnnotationsUpsert(t *testing.T) {
	t.Parallel()

	if len(spaceAnnotationAllColumns) == len(spaceAnnotationPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := SpaceAnnotation{}
	if err = randomize.Struct(seed, &o, spaceAnnotationDBTypes, true); err != nil {
		t.Errorf("Unable to randomize SpaceAnnotation struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(ctx, tx, false, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert SpaceAnnotation: %s", err)
	}

	count, err := SpaceAnnotations().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, spaceAnnotationDBTypes, false, spaceAnnotationPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize SpaceAnnotation struct: %s", err)
	}

	if err = o.Upsert(ctx, tx, true, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert SpaceAnnotation: %s", err)
	}

	count, err = SpaceAnnotations().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
