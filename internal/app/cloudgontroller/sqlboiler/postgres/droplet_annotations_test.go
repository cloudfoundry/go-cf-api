// Code generated by SQLBoiler 4.6.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"bytes"
	"context"
	"reflect"
	"testing"

	"github.com/volatiletech/randomize"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testDropletAnnotations(t *testing.T) {
	t.Parallel()

	query := DropletAnnotations()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testDropletAnnotationsDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DropletAnnotation{}
	if err = randomize.Struct(seed, o, dropletAnnotationDBTypes, true, dropletAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DropletAnnotation struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := DropletAnnotations().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testDropletAnnotationsQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DropletAnnotation{}
	if err = randomize.Struct(seed, o, dropletAnnotationDBTypes, true, dropletAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DropletAnnotation struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := DropletAnnotations().DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := DropletAnnotations().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testDropletAnnotationsSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DropletAnnotation{}
	if err = randomize.Struct(seed, o, dropletAnnotationDBTypes, true, dropletAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DropletAnnotation struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := DropletAnnotationSlice{o}

	if rowsAff, err := slice.DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := DropletAnnotations().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testDropletAnnotationsExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DropletAnnotation{}
	if err = randomize.Struct(seed, o, dropletAnnotationDBTypes, true, dropletAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DropletAnnotation struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := DropletAnnotationExists(ctx, tx, o.ID)
	if err != nil {
		t.Errorf("Unable to check if DropletAnnotation exists: %s", err)
	}
	if !e {
		t.Errorf("Expected DropletAnnotationExists to return true, but got false.")
	}
}

func testDropletAnnotationsFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DropletAnnotation{}
	if err = randomize.Struct(seed, o, dropletAnnotationDBTypes, true, dropletAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DropletAnnotation struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	dropletAnnotationFound, err := FindDropletAnnotation(ctx, tx, o.ID)
	if err != nil {
		t.Error(err)
	}

	if dropletAnnotationFound == nil {
		t.Error("want a record, got nil")
	}
}

func testDropletAnnotationsBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DropletAnnotation{}
	if err = randomize.Struct(seed, o, dropletAnnotationDBTypes, true, dropletAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DropletAnnotation struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = DropletAnnotations().Bind(ctx, tx, o); err != nil {
		t.Error(err)
	}
}

func testDropletAnnotationsOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DropletAnnotation{}
	if err = randomize.Struct(seed, o, dropletAnnotationDBTypes, true, dropletAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DropletAnnotation struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := DropletAnnotations().One(ctx, tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testDropletAnnotationsAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	dropletAnnotationOne := &DropletAnnotation{}
	dropletAnnotationTwo := &DropletAnnotation{}
	if err = randomize.Struct(seed, dropletAnnotationOne, dropletAnnotationDBTypes, false, dropletAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DropletAnnotation struct: %s", err)
	}
	if err = randomize.Struct(seed, dropletAnnotationTwo, dropletAnnotationDBTypes, false, dropletAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DropletAnnotation struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = dropletAnnotationOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = dropletAnnotationTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := DropletAnnotations().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testDropletAnnotationsCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	dropletAnnotationOne := &DropletAnnotation{}
	dropletAnnotationTwo := &DropletAnnotation{}
	if err = randomize.Struct(seed, dropletAnnotationOne, dropletAnnotationDBTypes, false, dropletAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DropletAnnotation struct: %s", err)
	}
	if err = randomize.Struct(seed, dropletAnnotationTwo, dropletAnnotationDBTypes, false, dropletAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DropletAnnotation struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = dropletAnnotationOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = dropletAnnotationTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := DropletAnnotations().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func dropletAnnotationBeforeInsertHook(ctx context.Context, e boil.ContextExecutor, o *DropletAnnotation) error {
	*o = DropletAnnotation{}
	return nil
}

func dropletAnnotationAfterInsertHook(ctx context.Context, e boil.ContextExecutor, o *DropletAnnotation) error {
	*o = DropletAnnotation{}
	return nil
}

func dropletAnnotationAfterSelectHook(ctx context.Context, e boil.ContextExecutor, o *DropletAnnotation) error {
	*o = DropletAnnotation{}
	return nil
}

func dropletAnnotationBeforeUpdateHook(ctx context.Context, e boil.ContextExecutor, o *DropletAnnotation) error {
	*o = DropletAnnotation{}
	return nil
}

func dropletAnnotationAfterUpdateHook(ctx context.Context, e boil.ContextExecutor, o *DropletAnnotation) error {
	*o = DropletAnnotation{}
	return nil
}

func dropletAnnotationBeforeDeleteHook(ctx context.Context, e boil.ContextExecutor, o *DropletAnnotation) error {
	*o = DropletAnnotation{}
	return nil
}

func dropletAnnotationAfterDeleteHook(ctx context.Context, e boil.ContextExecutor, o *DropletAnnotation) error {
	*o = DropletAnnotation{}
	return nil
}

func dropletAnnotationBeforeUpsertHook(ctx context.Context, e boil.ContextExecutor, o *DropletAnnotation) error {
	*o = DropletAnnotation{}
	return nil
}

func dropletAnnotationAfterUpsertHook(ctx context.Context, e boil.ContextExecutor, o *DropletAnnotation) error {
	*o = DropletAnnotation{}
	return nil
}

func testDropletAnnotationsHooks(t *testing.T) {
	t.Parallel()

	var err error

	ctx := context.Background()
	empty := &DropletAnnotation{}
	o := &DropletAnnotation{}

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, o, dropletAnnotationDBTypes, false); err != nil {
		t.Errorf("Unable to randomize DropletAnnotation object: %s", err)
	}

	AddDropletAnnotationHook(boil.BeforeInsertHook, dropletAnnotationBeforeInsertHook)
	if err = o.doBeforeInsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeInsertHook function to empty object, but got: %#v", o)
	}
	dropletAnnotationBeforeInsertHooks = []DropletAnnotationHook{}

	AddDropletAnnotationHook(boil.AfterInsertHook, dropletAnnotationAfterInsertHook)
	if err = o.doAfterInsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterInsertHook function to empty object, but got: %#v", o)
	}
	dropletAnnotationAfterInsertHooks = []DropletAnnotationHook{}

	AddDropletAnnotationHook(boil.AfterSelectHook, dropletAnnotationAfterSelectHook)
	if err = o.doAfterSelectHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterSelectHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterSelectHook function to empty object, but got: %#v", o)
	}
	dropletAnnotationAfterSelectHooks = []DropletAnnotationHook{}

	AddDropletAnnotationHook(boil.BeforeUpdateHook, dropletAnnotationBeforeUpdateHook)
	if err = o.doBeforeUpdateHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpdateHook function to empty object, but got: %#v", o)
	}
	dropletAnnotationBeforeUpdateHooks = []DropletAnnotationHook{}

	AddDropletAnnotationHook(boil.AfterUpdateHook, dropletAnnotationAfterUpdateHook)
	if err = o.doAfterUpdateHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpdateHook function to empty object, but got: %#v", o)
	}
	dropletAnnotationAfterUpdateHooks = []DropletAnnotationHook{}

	AddDropletAnnotationHook(boil.BeforeDeleteHook, dropletAnnotationBeforeDeleteHook)
	if err = o.doBeforeDeleteHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeDeleteHook function to empty object, but got: %#v", o)
	}
	dropletAnnotationBeforeDeleteHooks = []DropletAnnotationHook{}

	AddDropletAnnotationHook(boil.AfterDeleteHook, dropletAnnotationAfterDeleteHook)
	if err = o.doAfterDeleteHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterDeleteHook function to empty object, but got: %#v", o)
	}
	dropletAnnotationAfterDeleteHooks = []DropletAnnotationHook{}

	AddDropletAnnotationHook(boil.BeforeUpsertHook, dropletAnnotationBeforeUpsertHook)
	if err = o.doBeforeUpsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpsertHook function to empty object, but got: %#v", o)
	}
	dropletAnnotationBeforeUpsertHooks = []DropletAnnotationHook{}

	AddDropletAnnotationHook(boil.AfterUpsertHook, dropletAnnotationAfterUpsertHook)
	if err = o.doAfterUpsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpsertHook function to empty object, but got: %#v", o)
	}
	dropletAnnotationAfterUpsertHooks = []DropletAnnotationHook{}
}

func testDropletAnnotationsInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DropletAnnotation{}
	if err = randomize.Struct(seed, o, dropletAnnotationDBTypes, true, dropletAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DropletAnnotation struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := DropletAnnotations().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testDropletAnnotationsInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DropletAnnotation{}
	if err = randomize.Struct(seed, o, dropletAnnotationDBTypes, true); err != nil {
		t.Errorf("Unable to randomize DropletAnnotation struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Whitelist(dropletAnnotationColumnsWithoutDefault...)); err != nil {
		t.Error(err)
	}

	count, err := DropletAnnotations().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testDropletAnnotationToOneDropletUsingResource(t *testing.T) {
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var local DropletAnnotation
	var foreign Droplet

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, dropletAnnotationDBTypes, true, dropletAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DropletAnnotation struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, dropletDBTypes, false, dropletColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Droplet struct: %s", err)
	}

	if err := foreign.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	queries.Assign(&local.ResourceGUID, foreign.GUID)
	if err := local.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := local.Resource().One(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	if !queries.Equal(check.GUID, foreign.GUID) {
		t.Errorf("want: %v, got %v", foreign.GUID, check.GUID)
	}

	slice := DropletAnnotationSlice{&local}
	if err = local.L.LoadResource(ctx, tx, false, (*[]*DropletAnnotation)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Resource == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.Resource = nil
	if err = local.L.LoadResource(ctx, tx, true, &local, nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Resource == nil {
		t.Error("struct should have been eager loaded")
	}
}

func testDropletAnnotationToOneSetOpDropletUsingResource(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a DropletAnnotation
	var b, c Droplet

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, dropletAnnotationDBTypes, false, strmangle.SetComplement(dropletAnnotationPrimaryKeyColumns, dropletAnnotationColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, dropletDBTypes, false, strmangle.SetComplement(dropletPrimaryKeyColumns, dropletColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, dropletDBTypes, false, strmangle.SetComplement(dropletPrimaryKeyColumns, dropletColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*Droplet{&b, &c} {
		err = a.SetResource(ctx, tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.Resource != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.ResourceDropletAnnotations[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if !queries.Equal(a.ResourceGUID, x.GUID) {
			t.Error("foreign key was wrong value", a.ResourceGUID)
		}

		zero := reflect.Zero(reflect.TypeOf(a.ResourceGUID))
		reflect.Indirect(reflect.ValueOf(&a.ResourceGUID)).Set(zero)

		if err = a.Reload(ctx, tx); err != nil {
			t.Fatal("failed to reload", err)
		}

		if !queries.Equal(a.ResourceGUID, x.GUID) {
			t.Error("foreign key was wrong value", a.ResourceGUID, x.GUID)
		}
	}
}

func testDropletAnnotationToOneRemoveOpDropletUsingResource(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a DropletAnnotation
	var b Droplet

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, dropletAnnotationDBTypes, false, strmangle.SetComplement(dropletAnnotationPrimaryKeyColumns, dropletAnnotationColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, dropletDBTypes, false, strmangle.SetComplement(dropletPrimaryKeyColumns, dropletColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err = a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	if err = a.SetResource(ctx, tx, true, &b); err != nil {
		t.Fatal(err)
	}

	if err = a.RemoveResource(ctx, tx, &b); err != nil {
		t.Error("failed to remove relationship")
	}

	count, err := a.Resource().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 0 {
		t.Error("want no relationships remaining")
	}

	if a.R.Resource != nil {
		t.Error("R struct entry should be nil")
	}

	if !queries.IsValuerNil(a.ResourceGUID) {
		t.Error("foreign key value should be nil")
	}

	if len(b.R.ResourceDropletAnnotations) != 0 {
		t.Error("failed to remove a from b's relationships")
	}
}

func testDropletAnnotationsReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DropletAnnotation{}
	if err = randomize.Struct(seed, o, dropletAnnotationDBTypes, true, dropletAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DropletAnnotation struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testDropletAnnotationsReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DropletAnnotation{}
	if err = randomize.Struct(seed, o, dropletAnnotationDBTypes, true, dropletAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DropletAnnotation struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := DropletAnnotationSlice{o}

	if err = slice.ReloadAll(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testDropletAnnotationsSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DropletAnnotation{}
	if err = randomize.Struct(seed, o, dropletAnnotationDBTypes, true, dropletAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DropletAnnotation struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := DropletAnnotations().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	dropletAnnotationDBTypes = map[string]string{`ID`: `integer`, `GUID`: `text`, `CreatedAt`: `timestamp without time zone`, `UpdatedAt`: `timestamp without time zone`, `ResourceGUID`: `character varying`, `KeyPrefix`: `character varying`, `Key`: `character varying`, `Value`: `character varying`}
	_                        = bytes.MinRead
)

func testDropletAnnotationsUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(dropletAnnotationPrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(dropletAnnotationAllColumns) == len(dropletAnnotationPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &DropletAnnotation{}
	if err = randomize.Struct(seed, o, dropletAnnotationDBTypes, true, dropletAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DropletAnnotation struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := DropletAnnotations().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, dropletAnnotationDBTypes, true, dropletAnnotationPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize DropletAnnotation struct: %s", err)
	}

	if rowsAff, err := o.Update(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testDropletAnnotationsSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(dropletAnnotationAllColumns) == len(dropletAnnotationPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &DropletAnnotation{}
	if err = randomize.Struct(seed, o, dropletAnnotationDBTypes, true, dropletAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DropletAnnotation struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := DropletAnnotations().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, dropletAnnotationDBTypes, true, dropletAnnotationPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize DropletAnnotation struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(dropletAnnotationAllColumns, dropletAnnotationPrimaryKeyColumns) {
		fields = dropletAnnotationAllColumns
	} else {
		fields = strmangle.SetComplement(
			dropletAnnotationAllColumns,
			dropletAnnotationPrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := DropletAnnotationSlice{o}
	if rowsAff, err := slice.UpdateAll(ctx, tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testDropletAnnotationsUpsert(t *testing.T) {
	t.Parallel()

	if len(dropletAnnotationAllColumns) == len(dropletAnnotationPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := DropletAnnotation{}
	if err = randomize.Struct(seed, &o, dropletAnnotationDBTypes, true); err != nil {
		t.Errorf("Unable to randomize DropletAnnotation struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(ctx, tx, false, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert DropletAnnotation: %s", err)
	}

	count, err := DropletAnnotations().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, dropletAnnotationDBTypes, false, dropletAnnotationPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize DropletAnnotation struct: %s", err)
	}

	if err = o.Upsert(ctx, tx, true, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert DropletAnnotation: %s", err)
	}

	count, err = DropletAnnotations().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
