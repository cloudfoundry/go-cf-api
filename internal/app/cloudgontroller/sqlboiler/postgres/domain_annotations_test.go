// +build integration
// Code generated by SQLBoiler 4.6.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"bytes"
	"context"
	"reflect"
	"testing"

	"github.com/volatiletech/randomize"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testDomainAnnotations(t *testing.T) {
	t.Parallel()

	query := DomainAnnotations()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testDomainAnnotationsDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DomainAnnotation{}
	if err = randomize.Struct(seed, o, domainAnnotationDBTypes, true, domainAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DomainAnnotation struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := DomainAnnotations().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testDomainAnnotationsQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DomainAnnotation{}
	if err = randomize.Struct(seed, o, domainAnnotationDBTypes, true, domainAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DomainAnnotation struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := DomainAnnotations().DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := DomainAnnotations().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testDomainAnnotationsSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DomainAnnotation{}
	if err = randomize.Struct(seed, o, domainAnnotationDBTypes, true, domainAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DomainAnnotation struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := DomainAnnotationSlice{o}

	if rowsAff, err := slice.DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := DomainAnnotations().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testDomainAnnotationsExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DomainAnnotation{}
	if err = randomize.Struct(seed, o, domainAnnotationDBTypes, true, domainAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DomainAnnotation struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := DomainAnnotationExists(ctx, tx, o.ID)
	if err != nil {
		t.Errorf("Unable to check if DomainAnnotation exists: %s", err)
	}
	if !e {
		t.Errorf("Expected DomainAnnotationExists to return true, but got false.")
	}
}

func testDomainAnnotationsFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DomainAnnotation{}
	if err = randomize.Struct(seed, o, domainAnnotationDBTypes, true, domainAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DomainAnnotation struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	domainAnnotationFound, err := FindDomainAnnotation(ctx, tx, o.ID)
	if err != nil {
		t.Error(err)
	}

	if domainAnnotationFound == nil {
		t.Error("want a record, got nil")
	}
}

func testDomainAnnotationsBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DomainAnnotation{}
	if err = randomize.Struct(seed, o, domainAnnotationDBTypes, true, domainAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DomainAnnotation struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = DomainAnnotations().Bind(ctx, tx, o); err != nil {
		t.Error(err)
	}
}

func testDomainAnnotationsOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DomainAnnotation{}
	if err = randomize.Struct(seed, o, domainAnnotationDBTypes, true, domainAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DomainAnnotation struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := DomainAnnotations().One(ctx, tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testDomainAnnotationsAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	domainAnnotationOne := &DomainAnnotation{}
	domainAnnotationTwo := &DomainAnnotation{}
	if err = randomize.Struct(seed, domainAnnotationOne, domainAnnotationDBTypes, false, domainAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DomainAnnotation struct: %s", err)
	}
	if err = randomize.Struct(seed, domainAnnotationTwo, domainAnnotationDBTypes, false, domainAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DomainAnnotation struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = domainAnnotationOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = domainAnnotationTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := DomainAnnotations().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testDomainAnnotationsCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	domainAnnotationOne := &DomainAnnotation{}
	domainAnnotationTwo := &DomainAnnotation{}
	if err = randomize.Struct(seed, domainAnnotationOne, domainAnnotationDBTypes, false, domainAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DomainAnnotation struct: %s", err)
	}
	if err = randomize.Struct(seed, domainAnnotationTwo, domainAnnotationDBTypes, false, domainAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DomainAnnotation struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = domainAnnotationOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = domainAnnotationTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := DomainAnnotations().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func domainAnnotationBeforeInsertHook(ctx context.Context, e boil.ContextExecutor, o *DomainAnnotation) error {
	*o = DomainAnnotation{}
	return nil
}

func domainAnnotationAfterInsertHook(ctx context.Context, e boil.ContextExecutor, o *DomainAnnotation) error {
	*o = DomainAnnotation{}
	return nil
}

func domainAnnotationAfterSelectHook(ctx context.Context, e boil.ContextExecutor, o *DomainAnnotation) error {
	*o = DomainAnnotation{}
	return nil
}

func domainAnnotationBeforeUpdateHook(ctx context.Context, e boil.ContextExecutor, o *DomainAnnotation) error {
	*o = DomainAnnotation{}
	return nil
}

func domainAnnotationAfterUpdateHook(ctx context.Context, e boil.ContextExecutor, o *DomainAnnotation) error {
	*o = DomainAnnotation{}
	return nil
}

func domainAnnotationBeforeDeleteHook(ctx context.Context, e boil.ContextExecutor, o *DomainAnnotation) error {
	*o = DomainAnnotation{}
	return nil
}

func domainAnnotationAfterDeleteHook(ctx context.Context, e boil.ContextExecutor, o *DomainAnnotation) error {
	*o = DomainAnnotation{}
	return nil
}

func domainAnnotationBeforeUpsertHook(ctx context.Context, e boil.ContextExecutor, o *DomainAnnotation) error {
	*o = DomainAnnotation{}
	return nil
}

func domainAnnotationAfterUpsertHook(ctx context.Context, e boil.ContextExecutor, o *DomainAnnotation) error {
	*o = DomainAnnotation{}
	return nil
}

func testDomainAnnotationsHooks(t *testing.T) {
	t.Parallel()

	var err error

	ctx := context.Background()
	empty := &DomainAnnotation{}
	o := &DomainAnnotation{}

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, o, domainAnnotationDBTypes, false); err != nil {
		t.Errorf("Unable to randomize DomainAnnotation object: %s", err)
	}

	AddDomainAnnotationHook(boil.BeforeInsertHook, domainAnnotationBeforeInsertHook)
	if err = o.doBeforeInsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeInsertHook function to empty object, but got: %#v", o)
	}
	domainAnnotationBeforeInsertHooks = []DomainAnnotationHook{}

	AddDomainAnnotationHook(boil.AfterInsertHook, domainAnnotationAfterInsertHook)
	if err = o.doAfterInsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterInsertHook function to empty object, but got: %#v", o)
	}
	domainAnnotationAfterInsertHooks = []DomainAnnotationHook{}

	AddDomainAnnotationHook(boil.AfterSelectHook, domainAnnotationAfterSelectHook)
	if err = o.doAfterSelectHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterSelectHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterSelectHook function to empty object, but got: %#v", o)
	}
	domainAnnotationAfterSelectHooks = []DomainAnnotationHook{}

	AddDomainAnnotationHook(boil.BeforeUpdateHook, domainAnnotationBeforeUpdateHook)
	if err = o.doBeforeUpdateHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpdateHook function to empty object, but got: %#v", o)
	}
	domainAnnotationBeforeUpdateHooks = []DomainAnnotationHook{}

	AddDomainAnnotationHook(boil.AfterUpdateHook, domainAnnotationAfterUpdateHook)
	if err = o.doAfterUpdateHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpdateHook function to empty object, but got: %#v", o)
	}
	domainAnnotationAfterUpdateHooks = []DomainAnnotationHook{}

	AddDomainAnnotationHook(boil.BeforeDeleteHook, domainAnnotationBeforeDeleteHook)
	if err = o.doBeforeDeleteHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeDeleteHook function to empty object, but got: %#v", o)
	}
	domainAnnotationBeforeDeleteHooks = []DomainAnnotationHook{}

	AddDomainAnnotationHook(boil.AfterDeleteHook, domainAnnotationAfterDeleteHook)
	if err = o.doAfterDeleteHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterDeleteHook function to empty object, but got: %#v", o)
	}
	domainAnnotationAfterDeleteHooks = []DomainAnnotationHook{}

	AddDomainAnnotationHook(boil.BeforeUpsertHook, domainAnnotationBeforeUpsertHook)
	if err = o.doBeforeUpsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpsertHook function to empty object, but got: %#v", o)
	}
	domainAnnotationBeforeUpsertHooks = []DomainAnnotationHook{}

	AddDomainAnnotationHook(boil.AfterUpsertHook, domainAnnotationAfterUpsertHook)
	if err = o.doAfterUpsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpsertHook function to empty object, but got: %#v", o)
	}
	domainAnnotationAfterUpsertHooks = []DomainAnnotationHook{}
}

func testDomainAnnotationsInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DomainAnnotation{}
	if err = randomize.Struct(seed, o, domainAnnotationDBTypes, true, domainAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DomainAnnotation struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := DomainAnnotations().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testDomainAnnotationsInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DomainAnnotation{}
	if err = randomize.Struct(seed, o, domainAnnotationDBTypes, true); err != nil {
		t.Errorf("Unable to randomize DomainAnnotation struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Whitelist(domainAnnotationColumnsWithoutDefault...)); err != nil {
		t.Error(err)
	}

	count, err := DomainAnnotations().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testDomainAnnotationToOneDomainUsingResource(t *testing.T) {
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var local DomainAnnotation
	var foreign Domain

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, domainAnnotationDBTypes, true, domainAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DomainAnnotation struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, domainDBTypes, false, domainColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Domain struct: %s", err)
	}

	if err := foreign.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	queries.Assign(&local.ResourceGUID, foreign.GUID)
	if err := local.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := local.Resource().One(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	if !queries.Equal(check.GUID, foreign.GUID) {
		t.Errorf("want: %v, got %v", foreign.GUID, check.GUID)
	}

	slice := DomainAnnotationSlice{&local}
	if err = local.L.LoadResource(ctx, tx, false, (*[]*DomainAnnotation)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Resource == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.Resource = nil
	if err = local.L.LoadResource(ctx, tx, true, &local, nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Resource == nil {
		t.Error("struct should have been eager loaded")
	}
}

func testDomainAnnotationToOneSetOpDomainUsingResource(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a DomainAnnotation
	var b, c Domain

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, domainAnnotationDBTypes, false, strmangle.SetComplement(domainAnnotationPrimaryKeyColumns, domainAnnotationColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, domainDBTypes, false, strmangle.SetComplement(domainPrimaryKeyColumns, domainColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, domainDBTypes, false, strmangle.SetComplement(domainPrimaryKeyColumns, domainColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*Domain{&b, &c} {
		err = a.SetResource(ctx, tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.Resource != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.ResourceDomainAnnotations[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if !queries.Equal(a.ResourceGUID, x.GUID) {
			t.Error("foreign key was wrong value", a.ResourceGUID)
		}

		zero := reflect.Zero(reflect.TypeOf(a.ResourceGUID))
		reflect.Indirect(reflect.ValueOf(&a.ResourceGUID)).Set(zero)

		if err = a.Reload(ctx, tx); err != nil {
			t.Fatal("failed to reload", err)
		}

		if !queries.Equal(a.ResourceGUID, x.GUID) {
			t.Error("foreign key was wrong value", a.ResourceGUID, x.GUID)
		}
	}
}

func testDomainAnnotationToOneRemoveOpDomainUsingResource(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a DomainAnnotation
	var b Domain

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, domainAnnotationDBTypes, false, strmangle.SetComplement(domainAnnotationPrimaryKeyColumns, domainAnnotationColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, domainDBTypes, false, strmangle.SetComplement(domainPrimaryKeyColumns, domainColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err = a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	if err = a.SetResource(ctx, tx, true, &b); err != nil {
		t.Fatal(err)
	}

	if err = a.RemoveResource(ctx, tx, &b); err != nil {
		t.Error("failed to remove relationship")
	}

	count, err := a.Resource().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 0 {
		t.Error("want no relationships remaining")
	}

	if a.R.Resource != nil {
		t.Error("R struct entry should be nil")
	}

	if !queries.IsValuerNil(a.ResourceGUID) {
		t.Error("foreign key value should be nil")
	}

	if len(b.R.ResourceDomainAnnotations) != 0 {
		t.Error("failed to remove a from b's relationships")
	}
}

func testDomainAnnotationsReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DomainAnnotation{}
	if err = randomize.Struct(seed, o, domainAnnotationDBTypes, true, domainAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DomainAnnotation struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testDomainAnnotationsReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DomainAnnotation{}
	if err = randomize.Struct(seed, o, domainAnnotationDBTypes, true, domainAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DomainAnnotation struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := DomainAnnotationSlice{o}

	if err = slice.ReloadAll(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testDomainAnnotationsSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &DomainAnnotation{}
	if err = randomize.Struct(seed, o, domainAnnotationDBTypes, true, domainAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DomainAnnotation struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := DomainAnnotations().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	domainAnnotationDBTypes = map[string]string{`ID`: `integer`, `GUID`: `text`, `CreatedAt`: `timestamp without time zone`, `UpdatedAt`: `timestamp without time zone`, `ResourceGUID`: `character varying`, `KeyPrefix`: `character varying`, `Key`: `character varying`, `Value`: `character varying`}
	_                       = bytes.MinRead
)

func testDomainAnnotationsUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(domainAnnotationPrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(domainAnnotationAllColumns) == len(domainAnnotationPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &DomainAnnotation{}
	if err = randomize.Struct(seed, o, domainAnnotationDBTypes, true, domainAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DomainAnnotation struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := DomainAnnotations().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, domainAnnotationDBTypes, true, domainAnnotationPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize DomainAnnotation struct: %s", err)
	}

	if rowsAff, err := o.Update(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testDomainAnnotationsSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(domainAnnotationAllColumns) == len(domainAnnotationPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &DomainAnnotation{}
	if err = randomize.Struct(seed, o, domainAnnotationDBTypes, true, domainAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize DomainAnnotation struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := DomainAnnotations().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, domainAnnotationDBTypes, true, domainAnnotationPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize DomainAnnotation struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(domainAnnotationAllColumns, domainAnnotationPrimaryKeyColumns) {
		fields = domainAnnotationAllColumns
	} else {
		fields = strmangle.SetComplement(
			domainAnnotationAllColumns,
			domainAnnotationPrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := DomainAnnotationSlice{o}
	if rowsAff, err := slice.UpdateAll(ctx, tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testDomainAnnotationsUpsert(t *testing.T) {
	t.Parallel()

	if len(domainAnnotationAllColumns) == len(domainAnnotationPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := DomainAnnotation{}
	if err = randomize.Struct(seed, &o, domainAnnotationDBTypes, true); err != nil {
		t.Errorf("Unable to randomize DomainAnnotation struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(ctx, tx, false, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert DomainAnnotation: %s", err)
	}

	count, err := DomainAnnotations().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, domainAnnotationDBTypes, false, domainAnnotationPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize DomainAnnotation struct: %s", err)
	}

	if err = o.Upsert(ctx, tx, true, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert DomainAnnotation: %s", err)
	}

	count, err = DomainAnnotations().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
