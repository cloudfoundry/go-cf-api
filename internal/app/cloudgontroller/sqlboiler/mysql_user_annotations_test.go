// +build mysql_integration
// Code generated by SQLBoiler 4.6.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"bytes"
	"context"
	"reflect"
	"testing"

	"github.com/volatiletech/randomize"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testUserAnnotations(t *testing.T) {
	t.Parallel()

	query := UserAnnotations()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testUserAnnotationsDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserAnnotation{}
	if err = randomize.Struct(seed, o, userAnnotationDBTypes, true, userAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserAnnotation struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := UserAnnotations().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testUserAnnotationsQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserAnnotation{}
	if err = randomize.Struct(seed, o, userAnnotationDBTypes, true, userAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserAnnotation struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := UserAnnotations().DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := UserAnnotations().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testUserAnnotationsSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserAnnotation{}
	if err = randomize.Struct(seed, o, userAnnotationDBTypes, true, userAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserAnnotation struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := UserAnnotationSlice{o}

	if rowsAff, err := slice.DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := UserAnnotations().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testUserAnnotationsExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserAnnotation{}
	if err = randomize.Struct(seed, o, userAnnotationDBTypes, true, userAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserAnnotation struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := UserAnnotationExists(ctx, tx, o.ID)
	if err != nil {
		t.Errorf("Unable to check if UserAnnotation exists: %s", err)
	}
	if !e {
		t.Errorf("Expected UserAnnotationExists to return true, but got false.")
	}
}

func testUserAnnotationsFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserAnnotation{}
	if err = randomize.Struct(seed, o, userAnnotationDBTypes, true, userAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserAnnotation struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	userAnnotationFound, err := FindUserAnnotation(ctx, tx, o.ID)
	if err != nil {
		t.Error(err)
	}

	if userAnnotationFound == nil {
		t.Error("want a record, got nil")
	}
}

func testUserAnnotationsBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserAnnotation{}
	if err = randomize.Struct(seed, o, userAnnotationDBTypes, true, userAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserAnnotation struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = UserAnnotations().Bind(ctx, tx, o); err != nil {
		t.Error(err)
	}
}

func testUserAnnotationsOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserAnnotation{}
	if err = randomize.Struct(seed, o, userAnnotationDBTypes, true, userAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserAnnotation struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := UserAnnotations().One(ctx, tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testUserAnnotationsAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	userAnnotationOne := &UserAnnotation{}
	userAnnotationTwo := &UserAnnotation{}
	if err = randomize.Struct(seed, userAnnotationOne, userAnnotationDBTypes, false, userAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserAnnotation struct: %s", err)
	}
	if err = randomize.Struct(seed, userAnnotationTwo, userAnnotationDBTypes, false, userAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserAnnotation struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = userAnnotationOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = userAnnotationTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := UserAnnotations().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testUserAnnotationsCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	userAnnotationOne := &UserAnnotation{}
	userAnnotationTwo := &UserAnnotation{}
	if err = randomize.Struct(seed, userAnnotationOne, userAnnotationDBTypes, false, userAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserAnnotation struct: %s", err)
	}
	if err = randomize.Struct(seed, userAnnotationTwo, userAnnotationDBTypes, false, userAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserAnnotation struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = userAnnotationOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = userAnnotationTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := UserAnnotations().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func userAnnotationBeforeInsertHook(ctx context.Context, e boil.ContextExecutor, o *UserAnnotation) error {
	*o = UserAnnotation{}
	return nil
}

func userAnnotationAfterInsertHook(ctx context.Context, e boil.ContextExecutor, o *UserAnnotation) error {
	*o = UserAnnotation{}
	return nil
}

func userAnnotationAfterSelectHook(ctx context.Context, e boil.ContextExecutor, o *UserAnnotation) error {
	*o = UserAnnotation{}
	return nil
}

func userAnnotationBeforeUpdateHook(ctx context.Context, e boil.ContextExecutor, o *UserAnnotation) error {
	*o = UserAnnotation{}
	return nil
}

func userAnnotationAfterUpdateHook(ctx context.Context, e boil.ContextExecutor, o *UserAnnotation) error {
	*o = UserAnnotation{}
	return nil
}

func userAnnotationBeforeDeleteHook(ctx context.Context, e boil.ContextExecutor, o *UserAnnotation) error {
	*o = UserAnnotation{}
	return nil
}

func userAnnotationAfterDeleteHook(ctx context.Context, e boil.ContextExecutor, o *UserAnnotation) error {
	*o = UserAnnotation{}
	return nil
}

func userAnnotationBeforeUpsertHook(ctx context.Context, e boil.ContextExecutor, o *UserAnnotation) error {
	*o = UserAnnotation{}
	return nil
}

func userAnnotationAfterUpsertHook(ctx context.Context, e boil.ContextExecutor, o *UserAnnotation) error {
	*o = UserAnnotation{}
	return nil
}

func testUserAnnotationsHooks(t *testing.T) {
	t.Parallel()

	var err error

	ctx := context.Background()
	empty := &UserAnnotation{}
	o := &UserAnnotation{}

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, o, userAnnotationDBTypes, false); err != nil {
		t.Errorf("Unable to randomize UserAnnotation object: %s", err)
	}

	AddUserAnnotationHook(boil.BeforeInsertHook, userAnnotationBeforeInsertHook)
	if err = o.doBeforeInsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeInsertHook function to empty object, but got: %#v", o)
	}
	userAnnotationBeforeInsertHooks = []UserAnnotationHook{}

	AddUserAnnotationHook(boil.AfterInsertHook, userAnnotationAfterInsertHook)
	if err = o.doAfterInsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterInsertHook function to empty object, but got: %#v", o)
	}
	userAnnotationAfterInsertHooks = []UserAnnotationHook{}

	AddUserAnnotationHook(boil.AfterSelectHook, userAnnotationAfterSelectHook)
	if err = o.doAfterSelectHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterSelectHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterSelectHook function to empty object, but got: %#v", o)
	}
	userAnnotationAfterSelectHooks = []UserAnnotationHook{}

	AddUserAnnotationHook(boil.BeforeUpdateHook, userAnnotationBeforeUpdateHook)
	if err = o.doBeforeUpdateHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpdateHook function to empty object, but got: %#v", o)
	}
	userAnnotationBeforeUpdateHooks = []UserAnnotationHook{}

	AddUserAnnotationHook(boil.AfterUpdateHook, userAnnotationAfterUpdateHook)
	if err = o.doAfterUpdateHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpdateHook function to empty object, but got: %#v", o)
	}
	userAnnotationAfterUpdateHooks = []UserAnnotationHook{}

	AddUserAnnotationHook(boil.BeforeDeleteHook, userAnnotationBeforeDeleteHook)
	if err = o.doBeforeDeleteHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeDeleteHook function to empty object, but got: %#v", o)
	}
	userAnnotationBeforeDeleteHooks = []UserAnnotationHook{}

	AddUserAnnotationHook(boil.AfterDeleteHook, userAnnotationAfterDeleteHook)
	if err = o.doAfterDeleteHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterDeleteHook function to empty object, but got: %#v", o)
	}
	userAnnotationAfterDeleteHooks = []UserAnnotationHook{}

	AddUserAnnotationHook(boil.BeforeUpsertHook, userAnnotationBeforeUpsertHook)
	if err = o.doBeforeUpsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpsertHook function to empty object, but got: %#v", o)
	}
	userAnnotationBeforeUpsertHooks = []UserAnnotationHook{}

	AddUserAnnotationHook(boil.AfterUpsertHook, userAnnotationAfterUpsertHook)
	if err = o.doAfterUpsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpsertHook function to empty object, but got: %#v", o)
	}
	userAnnotationAfterUpsertHooks = []UserAnnotationHook{}
}

func testUserAnnotationsInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserAnnotation{}
	if err = randomize.Struct(seed, o, userAnnotationDBTypes, true, userAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserAnnotation struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := UserAnnotations().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testUserAnnotationsInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserAnnotation{}
	if err = randomize.Struct(seed, o, userAnnotationDBTypes, true); err != nil {
		t.Errorf("Unable to randomize UserAnnotation struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Whitelist(userAnnotationColumnsWithoutDefault...)); err != nil {
		t.Error(err)
	}

	count, err := UserAnnotations().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testUserAnnotationToOneUserUsingResource(t *testing.T) {
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var local UserAnnotation
	var foreign User

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, userAnnotationDBTypes, true, userAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserAnnotation struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, userDBTypes, false, userColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize User struct: %s", err)
	}

	if err := foreign.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	queries.Assign(&local.ResourceGUID, foreign.GUID)
	if err := local.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := local.Resource().One(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	if !queries.Equal(check.GUID, foreign.GUID) {
		t.Errorf("want: %v, got %v", foreign.GUID, check.GUID)
	}

	slice := UserAnnotationSlice{&local}
	if err = local.L.LoadResource(ctx, tx, false, (*[]*UserAnnotation)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Resource == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.Resource = nil
	if err = local.L.LoadResource(ctx, tx, true, &local, nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Resource == nil {
		t.Error("struct should have been eager loaded")
	}
}

func testUserAnnotationToOneSetOpUserUsingResource(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a UserAnnotation
	var b, c User

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, userAnnotationDBTypes, false, strmangle.SetComplement(userAnnotationPrimaryKeyColumns, userAnnotationColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, userDBTypes, false, strmangle.SetComplement(userPrimaryKeyColumns, userColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, userDBTypes, false, strmangle.SetComplement(userPrimaryKeyColumns, userColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*User{&b, &c} {
		err = a.SetResource(ctx, tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.Resource != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.ResourceUserAnnotations[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if !queries.Equal(a.ResourceGUID, x.GUID) {
			t.Error("foreign key was wrong value", a.ResourceGUID)
		}

		zero := reflect.Zero(reflect.TypeOf(a.ResourceGUID))
		reflect.Indirect(reflect.ValueOf(&a.ResourceGUID)).Set(zero)

		if err = a.Reload(ctx, tx); err != nil {
			t.Fatal("failed to reload", err)
		}

		if !queries.Equal(a.ResourceGUID, x.GUID) {
			t.Error("foreign key was wrong value", a.ResourceGUID, x.GUID)
		}
	}
}

func testUserAnnotationToOneRemoveOpUserUsingResource(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a UserAnnotation
	var b User

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, userAnnotationDBTypes, false, strmangle.SetComplement(userAnnotationPrimaryKeyColumns, userAnnotationColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, userDBTypes, false, strmangle.SetComplement(userPrimaryKeyColumns, userColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err = a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	if err = a.SetResource(ctx, tx, true, &b); err != nil {
		t.Fatal(err)
	}

	if err = a.RemoveResource(ctx, tx, &b); err != nil {
		t.Error("failed to remove relationship")
	}

	count, err := a.Resource().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 0 {
		t.Error("want no relationships remaining")
	}

	if a.R.Resource != nil {
		t.Error("R struct entry should be nil")
	}

	if !queries.IsValuerNil(a.ResourceGUID) {
		t.Error("foreign key value should be nil")
	}

	if len(b.R.ResourceUserAnnotations) != 0 {
		t.Error("failed to remove a from b's relationships")
	}
}

func testUserAnnotationsReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserAnnotation{}
	if err = randomize.Struct(seed, o, userAnnotationDBTypes, true, userAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserAnnotation struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testUserAnnotationsReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserAnnotation{}
	if err = randomize.Struct(seed, o, userAnnotationDBTypes, true, userAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserAnnotation struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := UserAnnotationSlice{o}

	if err = slice.ReloadAll(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testUserAnnotationsSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &UserAnnotation{}
	if err = randomize.Struct(seed, o, userAnnotationDBTypes, true, userAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserAnnotation struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := UserAnnotations().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	userAnnotationDBTypes = map[string]string{`ID`: `int`, `GUID`: `varchar`, `CreatedAt`: `timestamp`, `UpdatedAt`: `timestamp`, `ResourceGUID`: `varchar`, `KeyPrefix`: `varchar`, `Key`: `varchar`, `Value`: `varchar`}
	_                     = bytes.MinRead
)

func testUserAnnotationsUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(userAnnotationPrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(userAnnotationAllColumns) == len(userAnnotationPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &UserAnnotation{}
	if err = randomize.Struct(seed, o, userAnnotationDBTypes, true, userAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserAnnotation struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := UserAnnotations().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, userAnnotationDBTypes, true, userAnnotationPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize UserAnnotation struct: %s", err)
	}

	if rowsAff, err := o.Update(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testUserAnnotationsSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(userAnnotationAllColumns) == len(userAnnotationPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &UserAnnotation{}
	if err = randomize.Struct(seed, o, userAnnotationDBTypes, true, userAnnotationColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize UserAnnotation struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := UserAnnotations().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, userAnnotationDBTypes, true, userAnnotationPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize UserAnnotation struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(userAnnotationAllColumns, userAnnotationPrimaryKeyColumns) {
		fields = userAnnotationAllColumns
	} else {
		fields = strmangle.SetComplement(
			userAnnotationAllColumns,
			userAnnotationPrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := UserAnnotationSlice{o}
	if rowsAff, err := slice.UpdateAll(ctx, tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testUserAnnotationsUpsert(t *testing.T) {
	t.Parallel()

	if len(userAnnotationAllColumns) == len(userAnnotationPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}
	if len(mySQLUserAnnotationUniqueColumns) == 0 {
		t.Skip("Skipping table with no unique columns to conflict on")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := UserAnnotation{}
	if err = randomize.Struct(seed, &o, userAnnotationDBTypes, false); err != nil {
		t.Errorf("Unable to randomize UserAnnotation struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(ctx, tx, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert UserAnnotation: %s", err)
	}

	count, err := UserAnnotations().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, userAnnotationDBTypes, false, userAnnotationPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize UserAnnotation struct: %s", err)
	}

	if err = o.Upsert(ctx, tx, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert UserAnnotation: %s", err)
	}

	count, err = UserAnnotations().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
