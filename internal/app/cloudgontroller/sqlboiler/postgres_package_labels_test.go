// +build integration postgres
// Code generated by SQLBoiler 4.6.0 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package models

import (
	"bytes"
	"context"
	"reflect"
	"testing"

	"github.com/volatiletech/randomize"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testPackageLabels(t *testing.T) {
	t.Parallel()

	query := PackageLabels()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testPackageLabelsDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &PackageLabel{}
	if err = randomize.Struct(seed, o, packageLabelDBTypes, true, packageLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize PackageLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := PackageLabels().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testPackageLabelsQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &PackageLabel{}
	if err = randomize.Struct(seed, o, packageLabelDBTypes, true, packageLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize PackageLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := PackageLabels().DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := PackageLabels().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testPackageLabelsSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &PackageLabel{}
	if err = randomize.Struct(seed, o, packageLabelDBTypes, true, packageLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize PackageLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := PackageLabelSlice{o}

	if rowsAff, err := slice.DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := PackageLabels().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testPackageLabelsExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &PackageLabel{}
	if err = randomize.Struct(seed, o, packageLabelDBTypes, true, packageLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize PackageLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := PackageLabelExists(ctx, tx, o.ID)
	if err != nil {
		t.Errorf("Unable to check if PackageLabel exists: %s", err)
	}
	if !e {
		t.Errorf("Expected PackageLabelExists to return true, but got false.")
	}
}

func testPackageLabelsFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &PackageLabel{}
	if err = randomize.Struct(seed, o, packageLabelDBTypes, true, packageLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize PackageLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	packageLabelFound, err := FindPackageLabel(ctx, tx, o.ID)
	if err != nil {
		t.Error(err)
	}

	if packageLabelFound == nil {
		t.Error("want a record, got nil")
	}
}

func testPackageLabelsBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &PackageLabel{}
	if err = randomize.Struct(seed, o, packageLabelDBTypes, true, packageLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize PackageLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = PackageLabels().Bind(ctx, tx, o); err != nil {
		t.Error(err)
	}
}

func testPackageLabelsOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &PackageLabel{}
	if err = randomize.Struct(seed, o, packageLabelDBTypes, true, packageLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize PackageLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := PackageLabels().One(ctx, tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testPackageLabelsAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	packageLabelOne := &PackageLabel{}
	packageLabelTwo := &PackageLabel{}
	if err = randomize.Struct(seed, packageLabelOne, packageLabelDBTypes, false, packageLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize PackageLabel struct: %s", err)
	}
	if err = randomize.Struct(seed, packageLabelTwo, packageLabelDBTypes, false, packageLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize PackageLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = packageLabelOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = packageLabelTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := PackageLabels().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testPackageLabelsCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	packageLabelOne := &PackageLabel{}
	packageLabelTwo := &PackageLabel{}
	if err = randomize.Struct(seed, packageLabelOne, packageLabelDBTypes, false, packageLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize PackageLabel struct: %s", err)
	}
	if err = randomize.Struct(seed, packageLabelTwo, packageLabelDBTypes, false, packageLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize PackageLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = packageLabelOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = packageLabelTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := PackageLabels().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func packageLabelBeforeInsertHook(ctx context.Context, e boil.ContextExecutor, o *PackageLabel) error {
	*o = PackageLabel{}
	return nil
}

func packageLabelAfterInsertHook(ctx context.Context, e boil.ContextExecutor, o *PackageLabel) error {
	*o = PackageLabel{}
	return nil
}

func packageLabelAfterSelectHook(ctx context.Context, e boil.ContextExecutor, o *PackageLabel) error {
	*o = PackageLabel{}
	return nil
}

func packageLabelBeforeUpdateHook(ctx context.Context, e boil.ContextExecutor, o *PackageLabel) error {
	*o = PackageLabel{}
	return nil
}

func packageLabelAfterUpdateHook(ctx context.Context, e boil.ContextExecutor, o *PackageLabel) error {
	*o = PackageLabel{}
	return nil
}

func packageLabelBeforeDeleteHook(ctx context.Context, e boil.ContextExecutor, o *PackageLabel) error {
	*o = PackageLabel{}
	return nil
}

func packageLabelAfterDeleteHook(ctx context.Context, e boil.ContextExecutor, o *PackageLabel) error {
	*o = PackageLabel{}
	return nil
}

func packageLabelBeforeUpsertHook(ctx context.Context, e boil.ContextExecutor, o *PackageLabel) error {
	*o = PackageLabel{}
	return nil
}

func packageLabelAfterUpsertHook(ctx context.Context, e boil.ContextExecutor, o *PackageLabel) error {
	*o = PackageLabel{}
	return nil
}

func testPackageLabelsHooks(t *testing.T) {
	t.Parallel()

	var err error

	ctx := context.Background()
	empty := &PackageLabel{}
	o := &PackageLabel{}

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, o, packageLabelDBTypes, false); err != nil {
		t.Errorf("Unable to randomize PackageLabel object: %s", err)
	}

	AddPackageLabelHook(boil.BeforeInsertHook, packageLabelBeforeInsertHook)
	if err = o.doBeforeInsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeInsertHook function to empty object, but got: %#v", o)
	}
	packageLabelBeforeInsertHooks = []PackageLabelHook{}

	AddPackageLabelHook(boil.AfterInsertHook, packageLabelAfterInsertHook)
	if err = o.doAfterInsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterInsertHook function to empty object, but got: %#v", o)
	}
	packageLabelAfterInsertHooks = []PackageLabelHook{}

	AddPackageLabelHook(boil.AfterSelectHook, packageLabelAfterSelectHook)
	if err = o.doAfterSelectHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterSelectHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterSelectHook function to empty object, but got: %#v", o)
	}
	packageLabelAfterSelectHooks = []PackageLabelHook{}

	AddPackageLabelHook(boil.BeforeUpdateHook, packageLabelBeforeUpdateHook)
	if err = o.doBeforeUpdateHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpdateHook function to empty object, but got: %#v", o)
	}
	packageLabelBeforeUpdateHooks = []PackageLabelHook{}

	AddPackageLabelHook(boil.AfterUpdateHook, packageLabelAfterUpdateHook)
	if err = o.doAfterUpdateHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpdateHook function to empty object, but got: %#v", o)
	}
	packageLabelAfterUpdateHooks = []PackageLabelHook{}

	AddPackageLabelHook(boil.BeforeDeleteHook, packageLabelBeforeDeleteHook)
	if err = o.doBeforeDeleteHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeDeleteHook function to empty object, but got: %#v", o)
	}
	packageLabelBeforeDeleteHooks = []PackageLabelHook{}

	AddPackageLabelHook(boil.AfterDeleteHook, packageLabelAfterDeleteHook)
	if err = o.doAfterDeleteHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterDeleteHook function to empty object, but got: %#v", o)
	}
	packageLabelAfterDeleteHooks = []PackageLabelHook{}

	AddPackageLabelHook(boil.BeforeUpsertHook, packageLabelBeforeUpsertHook)
	if err = o.doBeforeUpsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpsertHook function to empty object, but got: %#v", o)
	}
	packageLabelBeforeUpsertHooks = []PackageLabelHook{}

	AddPackageLabelHook(boil.AfterUpsertHook, packageLabelAfterUpsertHook)
	if err = o.doAfterUpsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpsertHook function to empty object, but got: %#v", o)
	}
	packageLabelAfterUpsertHooks = []PackageLabelHook{}
}

func testPackageLabelsInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &PackageLabel{}
	if err = randomize.Struct(seed, o, packageLabelDBTypes, true, packageLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize PackageLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := PackageLabels().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testPackageLabelsInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &PackageLabel{}
	if err = randomize.Struct(seed, o, packageLabelDBTypes, true); err != nil {
		t.Errorf("Unable to randomize PackageLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Whitelist(packageLabelColumnsWithoutDefault...)); err != nil {
		t.Error(err)
	}

	count, err := PackageLabels().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testPackageLabelToOnePackageUsingResource(t *testing.T) {
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var local PackageLabel
	var foreign Package

	seed := randomize.NewSeed()
	if err := randomize.Struct(seed, &local, packageLabelDBTypes, true, packageLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize PackageLabel struct: %s", err)
	}
	if err := randomize.Struct(seed, &foreign, packageDBTypes, false, packageColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Package struct: %s", err)
	}

	if err := foreign.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	queries.Assign(&local.ResourceGUID, foreign.GUID)
	if err := local.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := local.Resource().One(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	if !queries.Equal(check.GUID, foreign.GUID) {
		t.Errorf("want: %v, got %v", foreign.GUID, check.GUID)
	}

	slice := PackageLabelSlice{&local}
	if err = local.L.LoadResource(ctx, tx, false, (*[]*PackageLabel)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Resource == nil {
		t.Error("struct should have been eager loaded")
	}

	local.R.Resource = nil
	if err = local.L.LoadResource(ctx, tx, true, &local, nil); err != nil {
		t.Fatal(err)
	}
	if local.R.Resource == nil {
		t.Error("struct should have been eager loaded")
	}
}

func testPackageLabelToOneSetOpPackageUsingResource(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a PackageLabel
	var b, c Package

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, packageLabelDBTypes, false, strmangle.SetComplement(packageLabelPrimaryKeyColumns, packageLabelColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, packageDBTypes, false, strmangle.SetComplement(packagePrimaryKeyColumns, packageColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, packageDBTypes, false, strmangle.SetComplement(packagePrimaryKeyColumns, packageColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	for i, x := range []*Package{&b, &c} {
		err = a.SetResource(ctx, tx, i != 0, x)
		if err != nil {
			t.Fatal(err)
		}

		if a.R.Resource != x {
			t.Error("relationship struct not set to correct value")
		}

		if x.R.ResourcePackageLabels[0] != &a {
			t.Error("failed to append to foreign relationship struct")
		}
		if !queries.Equal(a.ResourceGUID, x.GUID) {
			t.Error("foreign key was wrong value", a.ResourceGUID)
		}

		zero := reflect.Zero(reflect.TypeOf(a.ResourceGUID))
		reflect.Indirect(reflect.ValueOf(&a.ResourceGUID)).Set(zero)

		if err = a.Reload(ctx, tx); err != nil {
			t.Fatal("failed to reload", err)
		}

		if !queries.Equal(a.ResourceGUID, x.GUID) {
			t.Error("foreign key was wrong value", a.ResourceGUID, x.GUID)
		}
	}
}

func testPackageLabelToOneRemoveOpPackageUsingResource(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a PackageLabel
	var b Package

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, packageLabelDBTypes, false, strmangle.SetComplement(packageLabelPrimaryKeyColumns, packageLabelColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &b, packageDBTypes, false, strmangle.SetComplement(packagePrimaryKeyColumns, packageColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}

	if err = a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	if err = a.SetResource(ctx, tx, true, &b); err != nil {
		t.Fatal(err)
	}

	if err = a.RemoveResource(ctx, tx, &b); err != nil {
		t.Error("failed to remove relationship")
	}

	count, err := a.Resource().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 0 {
		t.Error("want no relationships remaining")
	}

	if a.R.Resource != nil {
		t.Error("R struct entry should be nil")
	}

	if !queries.IsValuerNil(a.ResourceGUID) {
		t.Error("foreign key value should be nil")
	}

	if len(b.R.ResourcePackageLabels) != 0 {
		t.Error("failed to remove a from b's relationships")
	}
}

func testPackageLabelsReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &PackageLabel{}
	if err = randomize.Struct(seed, o, packageLabelDBTypes, true, packageLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize PackageLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testPackageLabelsReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &PackageLabel{}
	if err = randomize.Struct(seed, o, packageLabelDBTypes, true, packageLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize PackageLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := PackageLabelSlice{o}

	if err = slice.ReloadAll(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testPackageLabelsSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &PackageLabel{}
	if err = randomize.Struct(seed, o, packageLabelDBTypes, true, packageLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize PackageLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := PackageLabels().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	packageLabelDBTypes = map[string]string{`ID`: `integer`, `GUID`: `text`, `CreatedAt`: `timestamp without time zone`, `UpdatedAt`: `timestamp without time zone`, `ResourceGUID`: `character varying`, `KeyPrefix`: `character varying`, `KeyName`: `character varying`, `Value`: `character varying`}
	_                   = bytes.MinRead
)

func testPackageLabelsUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(packageLabelPrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(packageLabelAllColumns) == len(packageLabelPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &PackageLabel{}
	if err = randomize.Struct(seed, o, packageLabelDBTypes, true, packageLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize PackageLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := PackageLabels().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, packageLabelDBTypes, true, packageLabelPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize PackageLabel struct: %s", err)
	}

	if rowsAff, err := o.Update(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testPackageLabelsSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(packageLabelAllColumns) == len(packageLabelPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &PackageLabel{}
	if err = randomize.Struct(seed, o, packageLabelDBTypes, true, packageLabelColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize PackageLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := PackageLabels().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, packageLabelDBTypes, true, packageLabelPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize PackageLabel struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(packageLabelAllColumns, packageLabelPrimaryKeyColumns) {
		fields = packageLabelAllColumns
	} else {
		fields = strmangle.SetComplement(
			packageLabelAllColumns,
			packageLabelPrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := PackageLabelSlice{o}
	if rowsAff, err := slice.UpdateAll(ctx, tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testPackageLabelsUpsert(t *testing.T) {
	t.Parallel()

	if len(packageLabelAllColumns) == len(packageLabelPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := PackageLabel{}
	if err = randomize.Struct(seed, &o, packageLabelDBTypes, true); err != nil {
		t.Errorf("Unable to randomize PackageLabel struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(ctx, tx, false, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert PackageLabel: %s", err)
	}

	count, err := PackageLabels().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, packageLabelDBTypes, false, packageLabelPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize PackageLabel struct: %s", err)
	}

	if err = o.Upsert(ctx, tx, true, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert PackageLabel: %s", err)
	}

	count, err = PackageLabels().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
